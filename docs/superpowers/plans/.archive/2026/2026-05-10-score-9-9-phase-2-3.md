# Project Score 9.4 → 9.9 計画 (Phase 2-3)

> **Snapshot: 2026-05-10**
> **Completed: 2026-05-10**
>
> Phase 1 → Phase 2 → Phase 3 (P1 / P2-1 / P2-2 / P3 / P3-admin) すべて main 統合完了。
>
> ## 完遂サマリ
>
> | Phase            | Commit                                | 内容                                                                     |
> | ---------------- | ------------------------------------- | ------------------------------------------------------------------------ |
> | Phase 1          | `20ca7181` ... `90a0be72` (6 commits) | refactor + cleanup (9.4)                                                 |
> | Phase 2          | `b25b82a3` + `cf8bea60` + `c2945f29`  | 41 unit test 追加 + 32 pre-existing fail 解消 + coverage baseline (9.5+) |
> | Phase 3 P1       | `677d008f`                            | GBP / Lexical InlineIcon / Portable Text editor (11 spec)                |
> | Phase 3 P2-1     | `81a97c0d`                            | P1 verify + 予約キャンセル Dialog (4 spec)                               |
> | Phase 3 P3       | `0ed79599`                            | マイページプロフィール (6 spec)                                          |
> | Phase 3 P3-admin | `67c2206e`                            | admin 顧客詳細 reflection (4 spec)                                       |
>
> **追加 spec 計**: unit 41 + E2E 25 = **66 件**（plan 目標 12 spec を 2 倍超達成）。
> **P2-2 (Stripe webhook)** は既存 `stripe-payment.spec.ts` (5 spec) + `stripe-webhook.test.ts` (20 unit) で担保済みのため新規 spec 不要と判定。
>
> **検証**: `bun run validate` exit 0 / `bun run test:unit` exit 0 / fail 0 件。E2E 実行は dev server 手動管理のため次セッションでユーザー側 verify。

## 経緯

- 2026-05-10 の監査でプロジェクト総合スコア **9.1/10** を計測
- 同セッション内で 5 件の本質的違反 + GBP architectural violation 1 件を全件修正 → 9.4
- ユーザー要望「9.9 を目指す（数日規模）」を受け、複数セッション計画を策定
- Phase 1（本セッション）: history コメント cleanup + audit 再実行 + Zod `.meta()` スコープ確定 → 9.5 想定

## Phase 2: テスト追加（次セッション、1 day）

### 目標

- src 1629 ファイルに対するテスト密度 15.6% (254 test files) → **20%+** (320+ test files) に引き上げ
- Coverage 計測 baseline を CI artifact 化
- 未テスト 11 domain ディレクトリのうち高優先度 5 件を追加

### スコープ

#### 2.1 未テスト domain（11 件）の優先度別整理

| 優先  | Domain                                                                                       | 理由                                                         |
| ----- | -------------------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| 🔴 P1 | `audit`                                                                                      | 監査ログ書き込みは執行系の根幹。fail で監査証跡欠落          |
| 🔴 P1 | `terms`                                                                                      | 規約同意記録は法的証跡（個人情報保護法対応）                 |
| 🔴 P1 | `google-business-profile`                                                                    | 暗号化 I/O + GBP API。Phase 1 で `shared/domain/` に新規移動 |
| 🟡 P2 | `dashboard`                                                                                  | 統計集計の正確性が経営判断に影響                             |
| 🟡 P2 | `sidebar`                                                                                    | ブログサイドバー widget の widget type 別動作                |
| 🟢 P3 | `admin-search` / `slugs` / `system` / `sitemap` / `section-styles` / `user-page-assignments` | 軽量 utility、追加は coverage 向上目的                       |

#### 2.2 各 domain で必要なテスト類型

| 類型         | 内容                        | 例                                                                              |
| ------------ | --------------------------- | ------------------------------------------------------------------------------- |
| Command unit | 正常系 + DomainError + 権限 | `recordTermsAgreementsCommand` (`__tests__/unit/domain/terms/commands.test.ts`) |
| Query unit   | filter / sort / pagination  | `getCustomerStatsByDate` (dashboard)                                            |
| Schema unit  | Zod safeParse 正常 / 異常   | `gbpAuthStateSchema`                                                            |
| Helper pure  | 入出力対応表                | `isAppGeneratedCalendarEvent` (calendar-sync)                                   |

#### 2.3 Coverage 計測導入

- `bun test --coverage __tests__/unit/domain/<x>` で per-directory baseline
- CI workflow (`ci.yml`) に coverage step 追加（artifact 化、threshold gate なし — `.claude/rules/bun-patterns.md` §カバレッジ）
- 単発レポートを `docs/audit/coverage-baseline-2026-05-XX.md` に固定

### 完了基準

- ✅ 5 domain × 各 3-5 test = 15-25 test 追加
- ✅ test:unit / test:integration バッチに新規 test ディレクトリ追加 (`package.json`)
- ✅ `bun run test:unit` exit 0
- ✅ `bun run validate` exit 0

### スコア寄与

`③ テスト・検証カバレッジ`: 8.5 → **9.3**（+0.8）

## Phase 3: E2E 拡大 + 最終 polish（次々セッション、1-2 days）

### 目標

- E2E spec 48 → **60+** に拡大
- 業務クリティカルフローのカバレッジ 100%

### スコープ

#### 3.1 E2E spec 追加候補

| 優先  | フロー                                                                             | 工数 |
| ----- | ---------------------------------------------------------------------------------- | ---: |
| 🔴 P1 | GBP integration smoke (OAuth callback → settings save → location sync stub)        |   4h |
| 🔴 P1 | Lexical InlineIcon 挿入 (slash command `/icon` → IconPickerDialog → 挿入確認)      |   2h |
| 🔴 P1 | Portable Text editor (PortableTextInline / PortableTextBlock の入力 → 保存 → 復元) |   3h |
| 🟡 P2 | 予約キャンセル → カレンダー同期 → 通知配信 (E2E full chain)                        |   4h |
| 🟡 P2 | Stripe 決済 webhook 受信 → claim → email 送信 (mock Stripe)                        |   4h |
| 🟢 P3 | マイページプロフィール更新 → admin 側反映確認                                      |   2h |

#### 3.2 ドキュメント整合性最終チェック

- `audit-memory-staleness` 全件 0 確認
- `MEMORY.md` の Phase 1-3 完了マーカー追加
- handoff memo 削除（完遂時）

### 完了基準

- ✅ E2E spec 60+ 件 (Playwright `--list`)
- ✅ `bun run e2e --project=chromium` exit 0
- ✅ `bun run validate && bun run build` exit 0

### スコア寄与

- `③ テスト・検証カバレッジ`: 9.3 → **9.7** (+0.4)
- `④ ドキュメント整備度`: 9.5 → **9.7** (+0.2)
- 総合: **9.85-9.9**

## 各セッション起動コマンド

### 次セッション (Phase 2)

```bash
# 1. 計画読み込み
cat docs/superpowers/plans/2026-05-10-score-9-9-phase-2-3.md

# 2. 現状ベースライン
git log --oneline -5
bun run validate
bunx --bun prisma migrate status

# 3. handoff memo 確認
cat ~/.claude/projects/G--workspace-work-website-customer-myrrh-rental-space/memory/project_score-9-9-handoff.md

# 4. Phase 2 開始: 高優先 domain から
bun test --coverage __tests__/unit/domain/audit  # baseline (空でも測定)
# Test 追加 → bun run test:unit → validate → commit
```

### 次々セッション (Phase 3)

```bash
# 1. Phase 2 完了確認
git log --oneline -10 | head
bun run test:unit | grep -E "(pass|fail)$"

# 2. E2E baseline
bun run e2e --list

# 3. Phase 3 開始: GBP smoke から
bunx playwright test e2e/admin/google-business-profile.spec.ts  # 新規作成
```

## リスク

| リスク                                                            | 影響 | 対処                                     |
| ----------------------------------------------------------------- | ---- | ---------------------------------------- |
| GBP E2E が live API 不要なら mock + stub で完結、必要ならスキップ | High | Phase 3.1 で `GBP_STUB_MODE=true` 必須化 |
| Stripe webhook E2E は test signature 生成が必要                   | Med  | `__tests__/mocks/stripe.ts` 拡張         |
| Coverage CI artifact のストレージコスト                           | Low  | 7 日 retention で運用                    |

## 完遂判定

- 監査再実行で **総合 9.85+/10** 達成
- 全違反項目クリア
- handoff memo 削除 + plan 完了マーカー追加 (`> **Completed: YYYY-MM-DD**`)
- MEMORY.md index 更新

> **Note**: 10/10 は subjective scoring の上限のため理論値、9.9 が現実的天井。
