# コードベース監査 2026-08-12 — 計画の整理（2026-08-15）

> **これは実装計画ではない。** 消化済み計画の置き換えと、残件の分類である。
> 状態の SSoT は [2026-08-12-codebase-audit-progress.md](2026-08-12-codebase-audit-progress.md)。
> 指摘本文は [2026-08-12-codebase-audit-findings.md](2026-08-12-codebase-audit-findings.md)。
> 棄却は [2026-08-12-codebase-audit-refuted.md](2026-08-12-codebase-audit-refuted.md)。
> 旧計画 `docs/superpowers/plans/2026-08-13-codebase-audit-remediation.md` は消化後に削除済み
> （[docs/README.md](../README.md) の plan lifecycle）。**復元しない。**

検証日: 2026-08-15。件数は見出し / 台帳行を機械突合した。手数えではない。

---

## 1. いまの結論

この監査の実装計画は終わっている。

| 問い                        | 答え                                                                                                            |
| --------------------------- | --------------------------------------------------------------------------------------------------------------- |
| 未着手の確定指摘はあるか    | **無い。** F-01〜F-133 から F-94 を除いた 132 件はすべて progress §2 にあり、未着手台帳は空。                   |
| 重大 / 高 / 中 / 低の未着手 | 0 / 0 / 0 / 0                                                                                                   |
| 棄却を再着手するか          | **しない。** R-01〜R-62。F-94 は R-62（R-03 の再掲）。                                                          |
| 構造の穴 A–J は残っているか | **残っていない。** A は 2026-08-15 の webhook 境界 5 波で閉じた。B–J は progress / 旧計画の実測どおり閉じ済み。 |
| 残件列に文がある = 未着手か | **違う。** 採らなかった判断・範囲外・検証ギャップ・注記。F-ID は済。                                            |
| 欠陥ゼロか                  | **言わない。** 監査は静的読解。走査は当時厳密 41%。実行時検証はしていない。                                     |

次のコード変更は、この監査の続きとしては無い。残るのは運用判断と、別監査で拾う話だけ。

---

## 2. 文書の役割（二重管理しない）

| ファイル                                                                      | 持つもの                                    | 持たないもの                         |
| ----------------------------------------------------------------------------- | ------------------------------------------- | ------------------------------------ |
| findings.md                                                                   | 2026-08-12 時点の指摘本文                   | 済/未の状態                          |
| progress.md                                                                   | 済んだ指摘・PR・残件列・F-01 ローテーション | 未着手台帳、計画タスク               |
| refuted.md                                                                    | 棄却 62 件と理由                            | 確定指摘                             |
| このファイル                                                                  | 計画の終了宣言と残件の分類                  | 新しい F-ID、実装手順                |
| `docs/superpowers/specs/2026-08-15-webhook-boundary-no-money-mocks-design.md` | Hole A の却下案と波の契約                   | タスク分解（wave plan は消化後削除） |

同じ件数を 2 箇所に置かない。progress §1 は §2 の行数から導く。

---

## 3. 突合結果（2026-08-15）

| 集合                  |                  件数 | 突合                                          |
| --------------------- | --------------------: | --------------------------------------------- |
| findings の `### F-*` |                   132 | F-01〜F-133、欠番は F-94 のみ                 |
| progress §2 の F 行   |                   132 | findings と集合一致。片方にしか無い ID は 0   |
| progress 深刻度       | 高 11 / 中 64 / 低 57 | 合計 132。§1 表と一致                         |
| refuted の `## R-*`   |                    62 | R-01〜R-62。欠番なし                          |
| F-94                  |             0（確定） | findings 見出しなし、progress なし、R-62 のみ |

findings の「低（58 件）」は F-94 移転前の見出しだった。正は 57 で、本文側を直した。

### 3.1 コードでの裏取り（2026-08-15 追記）

上は**文書同士の突合**であって、実装が入っている証明ではない。別途これを確かめた。

| 検査                             | 範囲                          | 結果                      |
| -------------------------------- | ----------------------------- | ------------------------- |
| progress §2 が参照する PR の状態 | 92 件（`gh pr view`）         | 全件 MERGED。open PR は 0 |
| 実コードの読み取り               | 24 / 132 ID（高 11 件は全件） | 台帳の記述と一致。齟齬 0  |
| architecture gate の実行         | 190 本                        | 190 passed / 0 failed     |

**残り 108 件は台帳の自己申告のまま**で、コードでは読んでいない。gate が通ることは
「gate が見ている範囲」の証明であって、全 F-ID の実装確認ではない。

---

## 4. 旧計画のフェーズと現在地

旧計画は「まだやっていないこと」の一覧だった。消化後の対応は次のとおり。

| フェーズ | 旧計画の内容             | 現在地                                                                             |
| -------- | ------------------------ | ---------------------------------------------------------------------------------- |
| 1        | F-01 鍵ローテーション    | 段 1–3 済。旧 Secret Manager version 1・2 の disable だけが運用判断（progress §3） |
| 2        | F-03 / F-05 の取りこぼし | #2286 で閉じた。残件列は空                                                         |
| 3        | 構造の穴 A–J             | 下表。A は webhook 境界 5 波 + leftover 2 PR で閉じた                              |
| 4        | 中 64                    | 全件クローズ                                                                       |
| 5        | 低 57                    | 全件クローズ。品質 Wave で残件列の切り捨てを直すか製品判断で閉じた                 |
| 6        | 未着手台帳               | 空のまま削除。復元しない                                                           |

台帳外（ID なし）: F-01 後続の無停止ローテーション、`paymentFailedAt`、構造の穴の第一歩、記録 PR。progress §2 末尾。

2026-08-15 の公式 leftover（F-104 カタログ税、Zod `taxRateType`、`savePaymentIntentId` の `deletedAt`、Hole A leftover）は PR #2335–#2340 で閉じた。イベントチケットは税込 `ticket.price` のまま（新 F-ID なし）。

---

## 5. 構造の穴 A–J

個別 F-ID を潰しても再発する、と旧計画が書いた 10 テーマ。閉じた根拠は旧計画 §4 と progress。詳細は git 履歴（`b6778edef^:docs/superpowers/plans/2026-08-13-codebase-audit-remediation.md`）。

| 穴  | 主題                                        | 状態   | 残してよいもの                                                                               |
| --- | ------------------------------------------- | ------ | -------------------------------------------------------------------------------------------- |
| A   | 金が動く経路に domain mock を挟まない       | 閉じた | cancelled orphan と Stripe 変換 unit。GCal / Resend / SwitchBot は決済を書かないので触らない |
| B   | cacheTag producer と Cache-Tag ヘッダの突合 | 閉じた | path 正規表現の実 URL マッチは範囲外（gate JSDoc）                                           |
| C   | EventTicket.unitSize の価格と定員           | 閉じた | 定員は `quantity = 人数`。DB trigger は未変更                                                |
| D   | feature OFF 中の webhook と cron            | 閉じた | webhook に `isFeatureEnabled` は足さない（意図）                                             |
| E   | 匿名化の PII 突き合わせ                     | 閉じた | `terms_agreements` だけ残してよい                                                            |
| F   | クーポン release の分散                     | 閉じた | `releaseCouponUsage` に集約済み                                                              |
| G   | 管理 page の loader 迂回                    | 閉じた | 確認された欠陥はゼロ。新規 gate は足していない                                               |
| H   | GCal 逆流の金額・クーポン                   | 閉じた | #2271                                                                                        |
| I   | gate 走査範囲と書込経路                     | 閉じた | #2272。`scanSync` 認識 + 下限 3 本                                                           |
| J   | waitlist session lock                       | 閉じた | 行リース。728354 は使わない                                                                  |

---

## 6. 残件列の分類（26 行）

progress §2 の残件列に文がある行。どれも **F-ID は済**。次の実装対象ではない。

### 6.1 採らなかった判断（再実装しない）

| ID    | 残件                                        | 意味                                             |
| ----- | ------------------------------------------- | ------------------------------------------------ |
| F-02  | 定員側は `quantity = 人数` で対処不要       | 価格式だけ直した。定員式は変えない               |
| F-08  | 削除順の入替は採らず                        | JSON 走査を生 SQL にしたまで                     |
| F-46  | `Event.source` 列は入れない                 | 削除反映ガードで足りる                           |
| F-51  | 列は足していない                            | FAQ 閲覧が `updated_at` を触らないようにしたまで |
| F-72  | 通常 purge の Retry-After は維持            | canary だけ `void` + `retry: false`              |
| F-83  | refund fixture は免除                       | seed へ移すと全テストの返金挙動が変わる          |
| F-113 | POST は token 検証後のまま                  | GET の DL rate limit だけ ownership 後へ移した   |
| F-119 | createEvent/updateEvent の spaceId は未変更 | 公開面の会場リンクだけ直した                     |
| F-124 | 列追加は採らず                              | `startTime` 変更時に `reminderSentAt` を戻すまで |
| F-131 | 呼び出し側の件数制限は正本にしない          | DeleteObjects の 1000 チャンクまで               |

品質 Wave で製品判断として閉じ、残件列は空のままのもの: F-56（`Math.round` / USD 移行しない）、F-63（既存重複行の DB 是正しない）、F-65（共有 cacheHandler を足さない）、F-80（全 resource 反転 + EXEMPT しない）。

### 6.2 注記（作業ではない）

F-27（旧 JSON の horizontal は vertical に戻る）、F-49（予約・イベント両側、テスト主張を反転）、F-84（public step は F-16）、F-92（F-115 と同一）、F-102（捨てていたのはスパイク通知の reject）、F-105（`/blog`・`/news` は当初から forward）、F-129（PDF / マイページは当初から和暦）、F-132（gallery / hero と同型）。

### 6.3 範囲外（この監査ではやらない）

| ID    | 残件                                                 | 次にやるなら                              |
| ----- | ---------------------------------------------------- | ----------------------------------------- |
| F-103 | mypage の `/contact`・`/spaces`・`/faq` ハードコード | 別依頼。ヘッダー Reserve CTA だけ直した   |
| F-127 | Calendar / GBP の HTTP 経路                          | 別依頼。`retry.ts` の gRPC 分岐だけ直した |
| F-130 | Lexical ツールバーの sub/sup ボタン                  | 別依頼。公開面のタグ許可だけ直した        |

### 6.4 検証ギャップ（コードは閉じた。実行していない）

| ID                 | 残件                                 | やるとき                                                               |
| ------------------ | ------------------------------------ | ---------------------------------------------------------------------- |
| F-17               | seed の 4 件を実行して確かめていない | `bun run setup` または `bun run db:reset`。`db:seed` 単体は失敗する    |
| F-20 / F-21 / F-22 | 実 GCP 未実行                        | 次の本番監査で `gcloud` を叩くとき。単体テストは母集合と判定を固定済み |

### 6.5 既知の限界（直していない。報告済み）

| ID   | 残件                   | 意味                                                         |
| ---- | ---------------------- | ------------------------------------------------------------ |
| F-43 | 取り分の読みが lock 外 | 自動返金の計算は直した。読みのロックは PR 本文に報告したまま |

---

## 7. 運用判断だけ残っているもの

コード変更は不要。やるなら人間が本番を触る。

| 項目                                   | 出典               | やらなくてもよいか                                                        |
| -------------------------------------- | ------------------ | ------------------------------------------------------------------------- |
| Secret Manager version 1・2 の disable | F-01 / progress §3 | **よい。** 漏洩値は受理対象から外れている。ロールバック余地を残すなら残す |
| 実 GCP 監査の実行                      | F-20〜22           | 次の本番監査まで待ってよい                                                |
| seed の実実行確認                      | F-17               | 破壊的。必要になったときだけ                                              |

---

## 8. この監査がやっていないこと

旧計画 §7 の範囲外は、今も範囲外。

- ブラウザ描画、Stripe / Google / SwitchBot の実レスポンス、DB 並行、メールの MUA 表示は検証していない。
- 当時の走査は厳密 41% / 緩和 64%。未読の中心は `__tests__/unit/*` と `src/app/(admin)`。
- 133 件中、著者が実コードで独立確認したのは 14 件。残りは検出と反証の二段構え。
- 「132 件を消化した = 欠陥が無くなった」ではない。

新しい欠陥は、この台帳に足さない。別監査として findings を増やす。

---

## 9. 参照の引き方

| 知りたいこと                     | 見る場所                                                                              |
| -------------------------------- | ------------------------------------------------------------------------------------- |
| ある F-ID は済んだか             | progress §2 に行があるか。無ければ確定指摘ではない                                    |
| 何が起き、どう直すつもりだったか | findings の該当見出し                                                                 |
| 同じ仮説を再提出していいか       | refuted。理由を読んでから                                                             |
| Hole A の最終形                  | webhook-boundary spec                                                                 |
| 旧計画の穴の実測文               | `git show b6778edef^:docs/superpowers/plans/2026-08-13-codebase-audit-remediation.md` |
