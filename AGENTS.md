## Learned User Preferences

- When breaking changes are allowed, prefer official-recommended clean-break designs over compatibility shims or per-model adapters.
- After a planned program wave finishes, continue recommended follow-up work without waiting to be asked.
- When fixing a class of issue, find similar occurrences and design to prevent recurrence. Do not leave ambiguities; investigate and verify thoroughly before proceeding.
- When a deliverable is merged to main while the user's checkout sits on another branch, say so explicitly and make the artifact locally accessible (exact `git show` path or a local copy); a remote link alone reads as "not found".
- For larger programs, plan so the work can proceed in parallel (e.g. git worktrees) without disturbing other ongoing work.
- When asked to execute a plan, carry it through the full ship cycle: commit, push, PR, and merge to main.

## Learned Workspace Facts

- Integration connection health is stored in the `IntegrationHealth` table and read/written only through `src/shared/domain/settings/connection-health.ts`. Auth failures go to ERROR immediately; transient failures need 3 consecutive failures; success auto-recovers to CONNECTED.
- Production deploys are manual: `.github/workflows/deploy-production.yml` is `workflow_dispatch` only (cost/wake control), so merging to main does not deploy code or run `terraform apply`. Dispatch it via Actions or `gh workflow run deploy-production.yml --ref main`. Cloud Monitoring log metrics, email notification channel, alert policies, and the public Cloud Run availability SLO live in `terraform/monitoring.tf` (`infra/monitoring/` YAML is gone); email alerts need GitHub secret `MONITORING_ALERT_EMAIL_TF` and a one-time GCP channel confirmation.
- Google Calendar integration: retry classification lives in `src/shared/lib/google-api/retry.ts` (retryable: 408/429/500/502/503/504); `events.insert` uses client-specified deterministic event IDs so POST retry is idempotent (409 treated as success). The webhook intentionally returns 200 even on processing failure (retry-storm prevention); failures surface via `IntegrationHealth` records, the admin Calendar StatusBanner error message, and the `google_calendar_sync_failure` log metric + alert.
- CSP in `src/proxy.ts` deliberately relaxes `style-src` with `'unsafe-inline'` in development only (per the official Next.js CSP guide): Next.js 16 devtools injects nonce-less `<style>` tags and has no nonce API, so strict style-src in dev only produces console noise. Production keeps nonce + hashes.
- A migration edited after being applied locally leaves the DB silently diverged — `prisma migrate status` still reports "up to date" while the actual table is missing or different. If the app errors on a table that migrations claim to have, rebuild the local DB with `bun run db:reset`. Local DB URLs are pinned to `127.0.0.1`, never `localhost`: on this machine `wslrelay.exe` zombie bindings squat `[::1]:5432`/`[::1]:5433`, so localhost→::1 connections get ECONNRESET for single-address pg clients while multi-address clients (Node autoSelectFamily) still connect (defaults, `.env.example`, `.env.local` all switched). Long-lived `myrrh_test` also accumulates committed rows that break count-asserting cron tests locally — reset the DB before blaming code.
- This repo enables Next.js 16 `cacheComponents: true` (next.config.ts), under which `Date.now()` and other unstable values read during prerender fail with a Blocking Route (`blocking-prerender-current-time`) error. The official fix is `await connection()` before the read; a layout `connection()` does not protect page segments (separate prerender entries), and `instant = false` does not clear sync IO errors. Admin dashboard guards in `src/app/(admin)/admin/(dashboard)/_shared/queries/_helpers.ts` call `connection()` first because `verifyAdminSession` → `recordAdminLoginSuccess` reads `Date.now()`.
- SwitchBot: Open Token / Secret Key are stored AES-256-GCM encrypted in the `settings_switchbot` singleton (not env); saving requires a 64-hex `ENCRYPTION_KEY` (local `.env.local` filled by `bun run setup` when empty; production GCP Secret Manager injected into both Cloud Run services; keys are independent; restart the human-owned dev server after changing it; an empty key surfaces as 「Open Tokenの暗号化に失敗しました」). There is no sandbox: CONFIRMED reservations issue a real keypad passcode for `Space.smartLockDeviceId` only (location default is new-space init, not an issue-time fallback; no auto-pick among multiple pads; no cross-location assign). Official Device List `keyList.id` is Integer; Prisma `switchbotKeyId` is Text — absorb at the API boundary. Webhook is canonical (Keypad Touch `createKey` omits `commandId`; correlate commandId → keyName → single PENDING); localhost webhook URLs cannot receive SwitchBot cloud events; Device List `keyList` can lag 120s+. Passcodes are omitted from confirmation email (link to reservation detail: member `/mypage/reservations/{id}`, guest `/reservation/status?token=`) and revealed only during reservation ± buffer (admin SwitchBot setting, default 15 min, 0–180, same minutes before and after).
- Renovate PRs self-rebase (`rebaseWhen: "behind-base-branch"` in `.github/renovate.json5`): do not run `gh pr update-branch` or click GitHub's "Update branch" on them. A foreign commit makes Renovate stop auto-rebasing that PR, post `prEditNotification` ("Edited/Blocked") comments, and subscribes the commit author to the PR thread (the root cause of a past renovate[bot] email flood). To force a rebase, use the rebase/retry checkbox in the PR body. Do not approve Dependency Dashboard override majors that exceed parent declared ranges (`docs/dependency-overrides.md`).
- Conform custom controls go through `src/shared/lib/conform/control.tsx` (`useFieldControl` / `useTypedControl` + `HiddenControlInput`). Do not import `useInputControl` / `useControl` / `unstable_useControl` from `@conform-to/react` in `src/**` — `useInputControl` looks up `document.forms` and warns when a Radix Portal delays the form by 1 render. Subscribe to values with `fields.X.value`. The import ban is `__tests__/unit/architecture/conform-use-control-wrapper.test.ts`.
- Better Auth 1.7 identifies `Account` by `(issuer, accountId)` (`@@unique([issuer, accountId])`). `prisma.account.create` must set `issuer`: credential `local:credential` (and `accountId = userId`), Google `https://accounts.google.com`, LINE `https://access.line.me`. `unlinkAccount` / `getAccessToken` take the local `Account.id` from `listUserAccounts`, not `providerId`.
- Zod validation messages are Japanese via zod-ja: `src/shared/lib/validations/zod-ja.ts` is loaded server-side from `src/instrumentation.ts` and client-side via the `ZodJaRegistrar` component (`zod-ja-registrar.tsx`) rendered by both root layouts.
- Run local scripts and hook scripts with bun, not node — this repo standardizes on bun and the user corrected node-based hook scripts. Keep Bun pins lockstep across `package.json` `packageManager` / `engines.bun`, Dockerfile `oven/bun`, and `@types/bun` (enforced by `runtime-version-contract`); Cloud Run Next.js runner stays Node (ADR 0005). The local shell is Windows PowerShell: bash-style HEREDOC does not work, so commit with plain `git commit -m "..."`. In parallel fix worktrees, `bun install` can dirty `bun.lock` — do not commit `bun.lock` changes from those worktrees. Bun 1.4.0 is the verified baseline (full audit 2026-08-22: `docs/audits/2026-08-22-bun-1.4-full-compliance-audit.md`): `bunx --bun` no longer auto-loads `.env` (invoke prisma only via `bun run db:*` so env inherits); `bun.lock` was deleted and regenerated via plain `bun install`, so it is the official-default lockfileVersion 2 (never hand-edit the version line — delete + reinstall regenerates it); and bun:test `.rejects`/`.resolves` require a native Promise — adopt thenables like PrismaPromise via `Promise.resolve()` before the matcher, while real-DB try/catch rejection workarounds are gone (use idiomatic `.rejects`; two deliberate try/catch exceptions remain in `singleton-row-guard` and `render-editor-state-fail-loud`).

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
