---
description: 管理画面の共通 UI コンポーネント・予約カレンダーの SSoT
paths:
  - "src/app/(admin)/**"
  - src/shared/lib/conform/**
---

# SSOT — 管理画面 UI コンポーネント

プロジェクト全体で単一定義を厳守する定数・シングルトン。ローカル再定義・重複定義は禁止。

## 予約カレンダー

| 定数/変数                                                    | 場所                                                       | メモ                                                                                                                                                                                                            |
| ------------------------------------------------------------ | ---------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CalendarView` / `CALENDAR_VIEWS` / `getValidCalendarView()` | `@/admin/lib/calendar/calendar-types`                      | 4 ビュー列挙 SSoT。**新 view 追加時は 5 箇所同時更新必須**: ① `CALENDAR_VIEWS` tuple ② `calendar-domain.ts` の 4 switch ③ `CalendarToolbar.VIEW_OPTIONS` ④ `CalendarViewWrapper` dispatch ⑤ 新規 view component |
| `ResourceView` (Resource Scheduler パターン)                 | `reservations/_components/calendar/views/ResourceView.tsx` | 時間軸 × スペース列 grid。同スペース・同時間の予約は物理的に重ならず Week View の重複崩壊問題を構造的に回避                                                                                                     |
| `getStatusColorClass()`                                      | `@/admin/lib/calendar/calendar-domain`                     | 予約ステータス → カレンダー bg + border-l + text 色クラス SSoT。ハードコード inline 上書き禁止                                                                                                                  |
| `EventCell` / `EventBadge`                                   | `reservations/_components/calendar/EventCell.tsx`          | 全ビュー共通のイベント描画 SSoT。新ビュー追加時もこの 2 component を必ず再利用（直書き禁止）                                                                                                                    |

## 共通コンポーネント

| 定数/変数                                                                                                                                                                                                          | 場所                                                                                                                                                                                                                                                                                                                              | メモ                                                                                                                                                                                                                                                                             |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CheckboxCell`                                                                                                                                                                                                     | `@/admin/components/table` (`_shared/components/table/CheckboxCell.tsx`)                                                                                                                                                                                                                                                          | admin table 全 checkbox の SSoT。44px ヒットエリア。`<input type="checkbox">` 直書き禁止                                                                                                                                                                                         |
| `Pagination` / `DEFAULT_PER_PAGE_OPTIONS`                                                                                                                                                                          | `@/admin/components/ui/Pagination`                                                                                                                                                                                                                                                                                                | 管理画面ページネーション SSoT。タブ系は `pageUrlKey` + `perPageUrlKey` を**必ずペアで**指定（片方欠落で drift silent bug）。`parseAsPerPage.withDefault(N)` と必ず一致させる                                                                                                     |
| `NavTabs` / `NavTabItem`                                                                                                                                                                                           | `@/admin/components/ui` (`_shared/components/ui/nav-tabs.tsx`)                                                                                                                                                                                                                                                                    | 管理画面のページ遷移型タブ SSoT（WAI-ARIA APG 準拠）。ローカル `<nav>` / `<ul>` 直書き禁止。`h-10` 等固定高さ指定禁止（`min-h-11` と矛盾）                                                                                                                                       |
| `IntegrationHealthAlert` / `IntegrationHealthAlertClient` / `getIntegrationHealthSummary`                                                                                                                          | `admin/(dashboard)/_components/IntegrationHealthAlert{.tsx,Client.tsx}` / `@/shared/domain/settings/api-key-queries`                                                                                                                                                                                                              | `/admin/settings` のみで表示する統合接続状態 alert。dashboard top には配置しない（ノイズ化防止）                                                                                                                                                                                 |
| `UserInfo`                                                                                                                                                                                                         | `admin/(dashboard)/_components/UserInfo.tsx`                                                                                                                                                                                                                                                                                      | サイドバー下部の user 識別 SSoT。TopBar 右端への重複表示禁止                                                                                                                                                                                                                     |
| `Skeleton` (admin)                                                                                                                                                                                                 | `@/admin/components/ui` (barrel) + `_shared/components/ui/skeleton.tsx`                                                                                                                                                                                                                                                           | 管理画面 loading.tsx / Suspense fallback の base primitive SSoT                                                                                                                                                                                                                  |
| `DetailLoading` / `FormLoading` / `EditorLoading`                                                                                                                                                                  | `_shared/components/{DetailLoading,FormLoading,EditorLoading}.tsx`                                                                                                                                                                                                                                                                | 管理画面 loading.tsx の 3 SSoT（→ `frontend/loading-skeleton.md`）。各 loading.tsx は 1 行 re-export。新規 admin form / editor ページ追加時は本パターン踏襲必須                                                                                                                  |
| `MediaPickerField`                                                                                                                                                                                                 | `@/admin/components/media-picker` (`_shared/components/media-picker/MediaPickerField.tsx`)                                                                                                                                                                                                                                        | conform `useInputControl()` 接続の単一画像選択 SSoT。URL `<Input>` 直書き禁止                                                                                                                                                                                                    |
| `.bg-checker` utility                                                                                                                                                                                              | `(admin)/_styles/admin.css` `@layer utilities`                                                                                                                                                                                                                                                                                    | 画像プレビュー用チェッカーボード背景の SSoT。透過 PNG / SVG の透過部分可視化（業界標準）                                                                                                                                                                                         |
| `useSingleMediaPicker` / `field.media({ accept })` / `AutoMediaField` / `MediaPreview` / `MediaAcceptType` / `createMediaGroupSchema` / `detectMediaSourceType`                                                    | `@/admin/hooks/use-media-picker` + `@/shared/lib/sections/field-registry` + `pages/[slug]/_sections/_components/auto-fields/AutoMediaField.tsx` + `@/admin/components/media-picker/MediaPreview.tsx` + `@/shared/lib/sections/types` + `@/shared/lib/sections/definitions/_shared/media` + `@/shared/lib/media/detect-media-type` | 管理画面の全メディア入力 UI 統合 SSoT（画像 / 動画 / 音声 / 文書すべて統一）。`MediaAcceptType` SSoT: `"image" \| "video" \| "image-or-video" \| "audio" \| "file" \| "any"`。Inspector / Plugin の挿入ダイアログで `useSingleMediaPicker` / `MediaPickerDialog` accept 経由必須 |
| `IconPickerField` / `IconPickerDialog` / `CuratedIcon` / `ICON_CATEGORIES` / `getCuratedIconComponent`                                                                                                             | `@/admin/components/icon-picker/{IconPickerField,IconPickerDialog}` + `@/shared/components/icon-curation/{component-map,CuratedIcon}` + `@/shared/lib/icon-curation`                                                                                                                                                              | 管理画面の `field.icon()` + Lexical FeatureIconList + NavigationItem.iconName の共通 SSoT（三層分離アーキテクチャ）。**新規キュレーション追加は 2 箇所同時更新必須**: ① `icon-curation.ts` ② `component-map.tsx`。curation list bypass 禁止                                      |
| `useTypedInputControl` / `getTypedFieldList` / `getTypedFieldset` / `asTypedField` / `asConformDefaultValue` / `asConformSubmissionValue` / `asConformButtonGetter` / `asConformLooseRecord` / `asConformFieldset` | `@/shared/lib/conform/typed-input-control`                                                                                                                                                                                                                                                                                        | conform `FieldMetadata<T>` generic invariance 境界 cast を helper 内部に集約した SSoT。呼び出し側で `as unknown as FieldMetadata<...>` 等を書くこと禁止（`architecture-boundaries.test.ts §5` で gate）                                                                          |
| `getEncryptionKey()`                                                                                                                                                                                               | `@/shared/lib/env/encryption`                                                                                                                                                                                                                                                                                                     | 暗号化マスター鍵（`ENCRYPTION_KEY`）取得 SSoT helper。未設定時は明示 error throw。test は `mock.module()` で差し替え可能                                                                                                                                                         |
| `tryAcquireCalendarSyncLock()` / `releaseCalendarSyncLock()` / `CALENDAR_SYNC_LOCK_ID`                                                                                                                             | `@/shared/domain/calendar-sync/locks`                                                                                                                                                                                                                                                                                             | PostgreSQL advisory lock SSoT（`CALENDAR_SYNC_LOCK_ID = 728349` 固定）。Cloud Run 複数インスタンス間の排他制御。新規 cron 排他ロックは同パターンで別 ID を採番                                                                                                                   |
| `PublishSwitch` (admin table binary status SSoT)                                                                                                                                                                   | `@/admin/components/ui` (`_shared/components/ui/PublishSwitch.tsx`)                                                                                                                                                                                                                                                               | binary `isPublished` / `isActive` の inline toggle SSoT（8 resource 統一済）。`toggleXxx*` DB 読込反転型禁止。**Coupon は `CouponStateToggle` 専用**                                                                                                                             |
| `CouponStateToggle` / `COUPON_STATUS_CONFIG`                                                                                                                                                                       | `@/admin/(dashboard)/coupons/_components/{CouponStateToggle,CouponStatusBadge}.tsx`                                                                                                                                                                                                                                               | Coupon 専用 binary 制御 + 派生 5 状態 component。新規 derived-state 系 toggle は本 pattern 踏襲                                                                                                                                                                                  |
| `<XxxStatusSelect>` family — `ReservationStatusSelect` / `PostStatusSelect` / `EventStatusSelect`                                                                                                                  | 各 admin 配下 `_components/<Xxx>StatusSelect.tsx`                                                                                                                                                                                                                                                                                 | 多状態（3+ states）status の inline Select pattern SSoT。Status 列の TableCell 内に直接配置。ActionDropdown 経由の publish / cancel / archive menu 禁止                                                                                                                          |
