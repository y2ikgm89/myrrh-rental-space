---
paths:
  - src/**
  - prisma/**
---

# SSOT 定数・シングルトン

プロジェクト全体で単一定義を厳守する定数・シングルトン一覧。ローカル再定義・重複定義は禁止。

## 認証・権限

| 定数/変数                                                                                       | 場所                                 | メモ                                                                                                                                                                                                                                                                                                                      |
| ----------------------------------------------------------------------------------------------- | ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `DASHBOARD_ROLES` / `ROLE_LABELS` / `ROLE_DESCRIPTIONS` / `isDashboardRole()` / `DashboardRole` | `@/shared/lib/admin-roles`           | client-safe Role SSoT。`admin-auth.ts`（server-only）が再 export。tuple のため `isDashboardRole()` 型ガード必須                                                                                                                                                                                                           |
| `INVITABLE_BY` / `getInvitableRoles()` / `canInviteRole()` / `canModifyUser()`                  | `@/shared/lib/admin-roles`           | client-safe RBAC 階層制御。SUPER_ADMIN→ADMIN/EDITOR/VIEWER、ADMIN→EDITOR/VIEWER のみ（特権昇格防止）                                                                                                                                                                                                                      |
| `adminAuth` / `customerAuth`                                                                    | `@/shared/lib/{admin,customer}-auth` | cookie prefix 分離。顧客は Google/LINE、`basePath: /api/customer-auth`                                                                                                                                                                                                                                                    |
| `Resource` / `Action` / `RESOURCE_LABELS`                                                       | `@/admin/lib/admin-resources`        | client-safe Resource SSoT。`permissions.ts` が再 export                                                                                                                                                                                                                                                                   |
| `TURNSTILE_ACTIONS` / `TurnstileAction` / `DEFAULT_TURNSTILE_APPEARANCE`                        | `@/shared/lib/turnstile-actions`     | client-safe Turnstile action SSoT（英数/`_`/`-`、最大32文字）。server 側 `expectedAction` 検証で同一値参照                                                                                                                                                                                                                |
| `LogoutButton` / `HeaderAuthSlot`                                                               | `@/public/components/{ui,layouts}/*` | 公開顧客ログアウト SSoT。`HeaderAuthSlot` は `"authenticated" \| "guest"` discriminated union。`signOut({ fetchOptions: { onSuccess: () => router.push + router.refresh } })` で PPR session 無効化。**マイページ等にローカル配置禁止** — ヘッダー右上 1 箇所（→ `auth-patterns.md`）                                     |
| `executeAdminMutationResult`                                                                    | `@/admin/lib/admin-action`           | 管理画面書き込み系 Server Action SSoT。実行順序は `execute → await afterSuccess → fireAndForget(logAction)` で不変。`await logAction` 化は cache invalidation スキップの silent regression。読み取り系は `_shared/queries/*` + API route（→ `server-actions/implementation.md` §executeAdminMutationResult 実行順序契約） |
| `fireAndForget` / `settleAllWithLogging`                                                        | `@/shared/lib/async-utils`           | 非クリティカル Promise の unhandled rejection 防止 SSoT。`fireAndForget` は単発（`.catch` で `logError`）、`settleAllWithLogging` は並列（`Promise.allSettled` + 個別 logError）。監査ログ / 通知生成 / メール送信の非ブロッキング実行で必須                                                                              |

## DB / Prisma

| 定数/変数                                                                          | 場所                                          | メモ                                                                                                                                                                                                                                 |
| ---------------------------------------------------------------------------------- | --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `prisma` / `basePrisma`                                                            | `@/shared/db/prisma`                          | `basePrisma` は Better Auth アダプター専用（`$extends` 前）                                                                                                                                                                          |
| `Prisma` 型 / Prisma enums（`Role` / `ReservationStatus` 等 34 種）                | `@/shared/lib/validations/enums/prisma-types` | client-safe gateway。`Prisma` 名前空間は型のみ再 export。**runtime sentinel（`JsonNull` / `DbNull` / `join` / `sql` / `raw`）は gateway から取得不可** — `shared/db` / `shared/domain` が `@generated/prisma/client` から直接 import |
| `*_STATUS_LABELS` / `AUDIT_ACTION_LABELS` / `PUBLISH_LABELS` / `getPublishLabel()` | `enums/helpers`                               | 全ステータス enum + boolean publish のラベル SSoT。UI でハードコード禁止                                                                                                                                                             |
| `NOTIFICATION_TYPE` / `isValidNotificationType`                                    | `enums/helpers`                               | DB VARCHAR 管理                                                                                                                                                                                                                      |
| `FaqItem.answer`                                                                   | `prisma/schema.prisma`                        | **プレーンテキスト単一列**（`@db.Text`）。管理は `/admin/faq` → `/admin/faq/[categoryId]` master-detail、CRUD は `FaqItemDialog` / `FaqCategoryDialog`。公開は `whitespace-pre-wrap`。Lexical 本文は Post/News/Terms/Section のみ    |

## 日時フォーマット / parser

| 定数/変数                                                                                | 場所                         | メモ                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ---------------------------------------------------------------------------------------- | ---------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `formatDate` / `formatDateShort` / `formatDateTimeShort` / `formatDateTimeFull`          | `@/shared/lib/date-format`   | 公開・管理共通の日時表示 SSoT。`new Intl.DateTimeFormat("ja-JP", ...)` ベース。コンポーネント内で `Intl.DateTimeFormat` 直接呼び出し禁止                                                                                                                                                                                                                                                                                                                                                        |
| `formatDateTimeLocalInJst(date)` / `parseDateTimeLocalAsJst(value)`                      | `@/shared/lib/date-format`   | **`<input type="datetime-local">` 用 JST 固定 SSoT**。表示は `Intl.DateTimeFormat("sv-SE", { timeZone: "Asia/Tokyo" })` で `"YYYY-MM-DDTHH:mm"`、parse は `+09:00` 付与で UTC Date 化（full ISO `Z` / `±HH:mm` 付きはそのまま委譲）。サーバ tz（Cloud Run UTC）/ ブラウザ tz 非依存。`new Date(localStr)` での parse / `format(d, "yyyy-MM-dd'T'HH:mm")` (date-fns) は両者ともサーバ・ブラウザ tz 依存で 9 時間ずれる silent bug の原因。**全 admin form の datetime-local は本 SSoT 経由必須** |
| `couponFormSchema.validFrom` / `eventFormBaseSchema.startTime` / `barFormSchema.startAt` | `@/shared/lib/validations/*` | datetime-local 値の Zod 契約 SSoT。`z.string().datetime({ local: true })` を統一採用（local + full ISO 両対応の Zod 4 公式オプション）。command 層で必ず `parseDateTimeLocalAsJst()` を通して UTC Date に変換（`new Date(data.x)` 直接禁止）                                                                                                                                                                                                                                                    |

## キャッシュ

| 定数/変数                                                                          | 場所                            | メモ                                                                             |
| ---------------------------------------------------------------------------------- | ------------------------------- | -------------------------------------------------------------------------------- |
| `CACHE_TAGS` / `getCacheTag` / `CACHE_LIFE`                                        | `@/shared/lib/constants`        | `CACHE_TAGS.SETTINGS` は廃止済 → 個別タグ                                        |
| `invalidateReservationCaches` / `invalidateEventCaches` / `invalidateReviewCaches` | `@/shared/lib/cache/*-cache.ts` | mutation 後のキャッシュ無効化 SSoT。ローカル `updateTag` 羅列禁止、helper を拡張 |

## 外部連携（Calendar / Storage）

| 定数/変数                                                                                            | 場所                                         | メモ                                                                                                                                                                                                                                                                                                                                                        |
| ---------------------------------------------------------------------------------------------------- | -------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `OUTBOUND_RESERVATION_MARKER` / `OUTBOUND_EVENT_MARKER` / `isAppGeneratedCalendarEvent`              | `@/shared/lib/calendar-sync/loop-prevention` | GCal outbound → inbound ループ防止 SSoT。outbound（`outbound.ts` / `event-outbound.ts`）が description 先頭に「予約ID:」「イベントID:」を埋め込み、inbound（`event-inbound.ts`）が `isAppGeneratedCalendarEvent(description)` で 1 本化判定してスキップ。literal を outbound / inbound で重複定義しない                                                     |
| `buildReservationCalendar` / `buildEventCalendar` / `buildAddToCalendarUrls` / `buildReservationUid` | `@/shared/lib/ical`                          | **RFC 5545 準拠 ICS SSoT**。`ical-generator` v10 + `@touch4it/ical-timezones`。UID 安定・SEQUENCE 管理（`icsSequence` を `{ increment: 1 }`）・METHOD:REQUEST/CANCEL・VTIMEZONE(Asia/Tokyo)。直接 `ical()` 呼び出し禁止。ICS DL は `/api/calendar/*` route（`data:` URL は Gmail ブロック）。UI は `AddToCalendar` Server Component（→ `ical-patterns.md`） |
| `STORAGE_PREFIXES` / `StoragePrefix`                                                                 | `@/shared/lib/r2/keys`                       | 画像ストレージの key prefix SSoT（`spaces` / `posts` / `site` / `media`）。Cloudflare R2 バケット内の仮想フォルダ名に対応。upload / delete の第 2 引数で使用                                                                                                                                                                                                |

## 公開 SEO / LocalBusiness JSON-LD

| 定数/変数                                                                                                                                                                     | 場所                                | メモ                                                                                                                  |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| `buildLocationLocalBusinessJsonLdData(location, options)` / `LocationLocalBusinessJsonLdData`                                                                                 | `@/public/lib/seo/location-json-ld` | per-location LocalBusiness JSON-LD ビルダー（pure function）。`getLocalBusinessJsonLdData()` を置き換え               |
| `getAllPublishedLocationsJsonLdData()`                                                                                                                                        | `@/public/lib/seo/location-json-ld` | `/access` 一覧ページ用。全公開拠点の JSON-LD 配列を `'use cache'` で返す                                              |
| `getLocationJsonLdDataBySlug(slug)`                                                                                                                                           | `@/public/lib/seo/location-json-ld` | `/access/[locationSlug]` 詳細ページ用。`includeBranchOf` を複数拠点時のみ true にする                                 |
| `Location.{latitude,longitude,googleBusinessPlaceId,googleReviewUrl,priceRange,paymentAccepted,phoneNumber,email,slug,postalCode,prefecture,city,streetAddress,buildingName}` | `prisma/schema.prisma`              | per-location MEO フィールド SSoT。Settings の旧 MEO フィールドからの移管済み。settings に同名フィールドを復活させない |

## ドメインロジック

| 定数/変数                                                                  | 場所                                                                                | メモ                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| -------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `formatEventVenue` / `formatEventAddress`                                  | `@/shared/domain/events/venue`                                                      | Event 会場表示 SSoT。`location` + `space` + `addressDetail` の 3 ソース合成。iCal LOCATION / Email / JSON-LD / EventCard / CSV / related events で共通利用。直接組み立て禁止                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Settings 連絡先住所（派生関数 SSoT）                                       | 4 派生並立                                                                          | **`Settings.address` カラムは廃止済（2026-05-01 migration `20260501033054_drop_settings_address`）**。表示用住所は構造化フィールド（`postalCode` / `prefecture` / `city` / `streetAddress` / `buildingName`）が SSoT。派生は `getBusinessInfo().address`（`@/public/data/business`、公開）/ `buildAddress(info)`（`@/shared/lib/terms-templates`）/ `extractBusinessInfo(settings)`（terms 管理画面 `terms/new/page.tsx`）/ seed 内 businessInfo（`prisma/seed.ts` の `seedTerms()`）の 4 箇所並立（将来 `formatSettingsAddress(settings)` への統合候補）。`Settings.address` の再導入禁止 — Stripe / Shopify / Google Address Validation API 全社が「構造化フィールド SSoT + 派生」パターン                                                                                                                |
| Location 連絡先・アクセス・駐車場（per-location SSoT）                     | `Location` モデル                                                                   | **`Settings.accessInfo` / `Settings.parkingInfo` カラムは廃止済（2026-05-01 migration `20260501041617_drop_settings_access_parking`）**。アクセス情報は `Location.accessLines Jsonb`（`string[]` 配列、useFieldArray + dnd-kit で管理、最大 20 件、`AccessLinesField` SSoT in `LocationForm.tsx`）、駐車場は `Location.parkingInfo String?`、電話/メール/住所は `Location.{phoneNumber,email,postalCode,prefecture,city,streetAddress,buildingName}` が SSoT。Settings 側に同名フィールドを復活させない（multi-location SSoT 違反）。Settings は全社共通の代表情報（`businessName` / `email` / `phoneNumber` 等）のみ。`Location.access` String 列も同日 `accessLines Jsonb` に配列化（migration `20260501040959_location_access_to_jsonb_array` + recovery `20260501041144_location_accesslines_resplit`） |
| `getDefaultSectionStyle` / `SectionStylePayload` / `DEFAULT_SECTION_STYLE` | `@/shared/domain/section-styles/types`                                              | code-owned section render style SSoT。Style Library / DB cascade は廃止し、section type ごとの fixed style を返す。public / preview renderer が共通利用                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `TermsDocument` / `TermsAgreement` / `recordTermsAgreementsCommand`        | `prisma/schema.prisma` + `@/shared/domain/terms` + `@/shared/lib/validations/terms` | 規約管理 SSoT（clean-break rebuild 2026-05-02、migration `20260502094243_create_terms_documents`）。旧 `Terms` + `TermsVersion` + `TermsAgreement` 3 テーブル / `TermsType` + `TermsStatus` enum 廃止 → `TermsDocument`（最新版直接保存、`type` VARCHAR(64) で柔軟化、`isPublished` boolean）+ `TermsAgreement`（同意時 `contentSnapshot` + sha256 hash で証跡化、`context` = `"reservation"`/`"inquiry"`/`"signup"`）。バージョン履歴は同意記録の snapshot で代替（Stripe / Notion パターン）。`recordTermsAgreementsCommand` は公開フォーム送信時に `fireAndForget` で非ブロッキング保存。管理画面: `/admin/terms`（Master-Detail）+ `/admin/terms/agreements`（監査）。`TERMS_AGREEMENT_CONTEXT` 定数で context 文字列を統一                                                                             |
| `getPagePreviewHref` / `getContentPreviewHref` / `getPublicPageHref`       | `@/shared/lib/preview-routes`                                                       | 管理画面 → preview / 公開ページへの URL 生成 SSoT。`PageActions` / `edit/page.tsx` / `use-preview` / `PageLivePreview` などから参照する。`/preview/pages/<slug>` / `/posts/preview/<slug>` / `/` のリテラル直書き禁止                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

## 管理画面 セクション編集（Dynamic Section Architecture）

| 定数/変数                                                                                                   | 場所                                                                                         | メモ                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ----------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `fieldRegistry` / `FieldMeta` / `field` helpers                                                             | `@/shared/lib/sections/field-registry`                                                       | Zod 4 `.register(fieldRegistry, meta)` ベースのフィールドメタデータ SSoT。`group: "content" \| "design" \| "advanced"` 必須。`.describe(JSON.stringify())` / `withMeta` / `field.text().pipe(z.string().max())` は全廃、`field.text("label", { maxLength: N, group: "design" })` パターンで統一。11 ヘルパー（text / textarea / number / boolean / select / color / image / url / icon / array / group）+ `field.dynamicSelect`（source: "postCategories" \| "faqCategories"、SC fetch + props 経由で AutoSelectField に options 注入、post-list / faq-list の categoryId が利用）。`subGroup: "text" \| "image" \| "button" \| "other"` で content グループ内意味別分類（AutoSectionForm が見出し付き描画）。AutoSectionForm が `fieldRegistry.get()` で読み取り Accordion 3 層 UI を自動生成 |
| `createButtonsArraySchema`                                                                                  | `@/shared/lib/sections/definitions/_shared/buttons`                                          | 全 buttons[] 配列の SSoT factory（text/url/variant/size/iconName/openInNewTab/customBackgroundColor/customTextColor + URL uniqueness refine）。cta / hero / hero-parallax が利用。新規 buttons[] フィールドは独自 inline schema 禁止、必ず factory 経由                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `createImageGroupSchema` / `createCompactImageGroupSchema`                                                  | `@/shared/lib/sections/definitions/_shared/image`                                            | 画像メタの構造化 SSoT（`{url, alt, caption?}` group）。hero / hero-parallax / concept は full 版、testimonial.items[] は compact 版（caption 不要）。新規画像フィールドは String 単独禁止、必ず factory 経由                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `sectionLayoutSchema` / `LAYOUT_PADDING_VALUES` / `LAYOUT_CONTAINER_WIDTH_VALUES` / `LAYOUT_ANIMATE_VALUES` | `@/shared/lib/sections/definitions/_shared/layout`                                           | 全 21 sections 共通の layout/visibility 設定 SSoT（padding / containerWidth / hideOnMobile / hideOnDesktop / animateOnScroll）。`group: "design"` で AutoSectionForm Accordion「デザイン」内に表示。新規 section schema は `layout: sectionLayoutSchema` 注入必須。**inner field 名衝突回避** — per-section の内部レイアウト field は `displayLayout` / `contentLayout` / `gridLayout` / `itemLayout` 等の semantic 名にする（key `layout` は共通 group 専用）                                                                                                                                                                                                                                                                                                                                 |
| `SectionWrapper`                                                                                            | `@/public/components/sections/SectionWrapper`                                                | 全 21 sections の外側責務（padding / containerWidth / visibility / animation）統一 SSoT。section component 内側のみ自身で描画、外側は wrapper 委譲。Hero / HeroParallax / page-hero の独自 svh 高さ制御は wrapper 内で継続                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Section レジストリ全体数                                                                                    | `@/shared/lib/sections/registry`                                                             | **21**（page-hero + 20 標準。homepage-\* は 2026-05-05 Phase 2 で完全削除、reservation-form は 2026-05-06 Phase 5 で追加）。新規 type 追加時は `getAllSectionDefinitions().length` を期待する `__tests__/unit/domain/sections/registry.test.ts` の数値とカテゴリ分布期待値を同時更新                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Section variant enum 拡張（`displayLayout` 等）                                                             | `definitions/<type>/schema.ts` + `validations/section.ts` + `validations/section-options.ts` | **3 箇所同時更新必須**: ① field-registry schema の `as const` 配列 ② v3 schema の `z.enum(values)` ③ `*Values` const + `*Labels` Record。1 つでも欠落すると TS2367（型重なりなし）/ TS2741（property missing）の silent bug。Phase 4-B（space-list catalog）/ 4-C（post-list / news-list archive）で確立                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `PAGE_TEMPLATES` / `getPageTemplate` / `resolveTemplateForSlug`                                             | `@/shared/lib/sections/page-templates`                                                       | Page Template SSoT（Phase 1 で導入、Phase 6 で admin UI に効かせ済）。各 Page slug に対応する template id（home / content / access / contact / faq / news-archive / blog-archive / events-archive / spaces-archive / reservation / custom）を持ち、`allowedSectionTypes` で AddSectionDialog をフィルタ、`requiredSectionTypes` で SectionListItem の削除メニューを disabled 化。`Page.template String @db.VarChar(64)` 列が DB SSoT、フロントエンド配線は `PageEditor` + `SectionListSidebar`                                                                                                                                                                                                                                                                                                 |
| `PAGE_TEMPLATES.requiredSectionTypes` ↔ `DEFAULT_PAGE_SECTIONS` 整合                                        | `@/shared/lib/sections/page-templates` + `@/shared/lib/constants/default-page-sections`      | requiredSectionTypes に含まれる type は同 slug の DEFAULT_PAGE_SECTIONS 配列にも含めること。**例外**: `home.page-hero` は seed.ts で order=-1 固定挿入する別 SSoT 経路のため defaults に含めない。drift があると新規環境 seed → 公開 render 時に必須 section が出ない silent bug（Phase 7 で contact-form 欠落を検出 + 修正、commit `2fd00aba`）                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `SectionRenderer.searchParams` / `SectionRenderer.pageSlug`                                                 | `@/public/components/sections/section-renderer`                                              | 公開 page.tsx → section への page-context 流通 SSoT。`searchParams?: Promise<SearchParams>` は server-side filtering / pagination が必要な variant（space-list catalog / post-list archive / news-list archive / reservation-form）で parse、`pageSlug?: string` は post-list archive sidebar 表示判定など page-template-aware section で `getPageShowSidebar(pageSlug)` 等に流す。全 11 公開 page.tsx + ManagedPageSections (preview / `[...segments]`) で配線済み                                                                                                                                                                                                                                                                                                                            |
| Section schema 重複（SSoT 違反、未統合）                                                                    | canonical: `definitions/<type>/schema.ts` / レガシー: `validations/section.ts`               | **canonical は `definitions/<type>/schema.ts`**。`validations/section.ts:470-` 等の同名 export はレガシー重複で型契約が drift（旧は `viewAllUrl: Route` / `title.min(1)` 等、canonical は `string` / `.default("")`）。`section-defaults.ts` の `getXxxConfig` は旧側を import しており、`createTypedConfigGetterFromSchema` は **fallback `safeParse({})` 成立**を前提にする（required field には必ず `.default()`）。修正時は両側を同期更新するか、旧スキーマに `.default()` を追加して fallback 契約を維持する。完全統合は ~17 component の型修正（`viewAllUrl: Route` 依存等）を伴うため未着手。実例: 2026-05-07 home CTA で `title.min(1)` + DB の stale `animateOnScroll: true` 二段 fail → SectionRenderer throw。ctaConfigSchema.title に `.default("")` を入れて修復                  |

## Lexical / 記事表示

| 定数/変数                                                                              | 場所                                         | メモ                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| -------------------------------------------------------------------------------------- | -------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ArticleLayout` / `ArticleHeader` / `ArticleFooter` / `ArticleTagList`                 | `@/public/components/{layouts,ui}/article-*` | 公開記事詳細（posts/news/preview）の統一ラッパー SSoT。`<article>` 末尾に個別 border ブロックを重ねない                                                                                                                                                                                                                                                                                                                                                                                |
| `extractHeadingsFromHtml` / `injectHeadingAnchors` / `slugifyHeading` / `HeadingEntry` | `@/shared/lib/html/extract-headings`         | 公開記事詳細の目次生成 SSoT（GFM / rehype-slug 互換）。`contentHtml` を canonical とし、h2/h3 抽出 + id 属性自動付与を **同一純粋関数で SSR / Client（SanitizedHtml）両側に保証**。CJK 文字保持、既存 id 尊重、重複は `-2/-3` 採番。posts / news / terms 共通                                                                                                                                                                                                                          |
| `CustomHeadingNode` / `anchorIdState` / `HeadingAnchorPlugin`                          | `@/admin/.../lexical/nodes,plugins`          | `HeadingNode` の NodeState 拡張 + Node Replacement。`HeadingAnchorPlugin` が `generateUniqueSlug` で `anchorId` 自動生成                                                                                                                                                                                                                                                                                                                                                               |
| `$getSelectionBlockNodes` / `$isMultiBlockSelection`                                   | `@/admin/.../lexical/lib/selection-helpers`  | 選択の「ブロック粒度」を求める SSoT。deepest common ancestor の直接 block-level 子を返す（WordPress Gutenberg の `getCommonRootClientID` 等価）。Group ネストに対応: Root 直下選択 → Root 子、Group 内選択 → Group 子。Floating Text FT（単一）↔ Block FT（複数）の排他制御、`GroupPlugin` から参照。ローカル再実装禁止                                                                                                                                                                |
| `EDITORIAL_PROSE_CLASSES` / `EDITOR_PROSE_CLASSES`                                     | `@/shared/lib/styles/prose`                  | 公開 `Prose` Primitive と Lexical エディタ（ContentEditable 親 div）の prose スタイル SSoT。Tailwind Typography v4 `prose-*` variant + Cormorant Garamond serif heading（light）+ bronze accent link + italic blockquote。Lexical `theme.ts` の heading/paragraph/quote/link/list utility は削除し外側 prose に委譲（編集中とプレビュー完全一致、WordPress Gutenberg / Lexical Playground 公式パターン）。直接書きクラスセット禁止、`Prose variant="editorial"` は `drop-cap` のみ追加 |

## 公開 UI / スクロール / アニメ

| 定数/変数                                                             | 場所                                                                      | メモ                                                                                                                                                                                                                                |
| --------------------------------------------------------------------- | ------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `scrollToElement` / `scrollToElementById` / `scrollToTop`             | `@/public/lib/scroll`                                                     | `--header-height` 補正 + `prefers-reduced-motion` で `behavior: "instant"` 切替                                                                                                                                                     |
| `ScrollReveal` / `ScrollRevealGroup`                                  | `@/public/components/animations/scroll-reveal`                            | 入場演出 SSoT。単一要素は `ScrollReveal`（Hero/CTA）、`.map` リストは `ScrollRevealGroup`（1 ScrollTrigger + stagger）。個別 wrap は fold 外 opacity:0 待機の silent bug（→ `frontend/gsap/scroll-trigger.md`）                     |
| `CARD_GRID_COLS_MAP` / `getCardGridColsClass`                         | `@/public/lib/section-style-maps`                                         | 公開 Section（SpaceShowcase / SpaceList / PostList）のカード列数 SSoT。viewport breakpoint ではなく `@container` + `@md:grid-cols-2 @3xl:grid-cols-3` の container query variants で定義。consumer 側で親に `@container` を付与必須 |
| `GRID_COLS_MAP` / `GALLERY_GRID_COLS_MAP`                             | `@/public/lib/section-style-maps`                                         | InstagramSection / NewsListSection (cardLayout) / GallerySection の列数 SSoT。CARD_GRID_COLS_MAP と同様 `@md:`/`@3xl:` Container Queries 採用、viewport breakpoint 復活禁止                                                         |
| `HERO_PARALLAX_HEIGHT_MAP` / `StandardHeroSection.HEIGHT_MAP`         | `@/public/lib/section-style-maps` / `_components/StandardHeroSection.tsx` | Hero 高さ SSoT。`min-h-[var(--hero-min-height-sm\|--hero-min-height\|--hero-min-height-lg)]` で `@theme` token 参照、arbitrary svh 復活禁止                                                                                         |
| `getTitleClasses` / `getTitleStyle` / `getTextStyle` / `titleSizeMap` | `@/public/components/sections/section-style-helpers`                      | section title / text style 算出 SSoT（pure module、`"use client"` なし）。Server / Client 両方から import 可。`SectionWrapper.tsx` からの re-export 禁止（SC/CC 境界違反の再発防止、2026-05-05 分離）                               |

## マイページ 共通コンポーネント

| 定数/変数                 | 場所                                               | メモ                                                                                                                                                              |
| ------------------------- | -------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `IncompleteProfileNotice` | `mypage/_components/incomplete-profile-notice.tsx` | 顧客プロフィール未完成時の警告 SSoT。layout で MypageNav 直後に配置、page 個別実装禁止（`isCustomerProfileComplete` で内部判定して `null` 返却）                  |
| `MypageSkeleton`          | `mypage/_components/mypage-skeleton.tsx`           | mypage `loading.tsx` 共通 placeholder。variant: `list` (3 cards) / `detail` (header + 2 blocks) / `form` (4 inputs + button)、単一 spinner 禁止、`aria-busy` 内蔵 |
| `ReservationTabs`         | `mypage/_components/reservation-tabs.tsx`          | 予約 active/past Tabs 分離 SSoT。Radix Tabs + 件数 badge + `data-[state=inactive]:hidden` の forceMount パターン                                                  |

## 管理画面 共通コンポーネント

| 定数/変数                                                                                 | 場所                                                                                                                 | メモ                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ----------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CheckboxCell`                                                                            | `@/admin/components/table` (`_shared/components/table/CheckboxCell.tsx`)                                             | admin table 全 checkbox の SSoT。`<label className="inline-flex min-h-11 min-w-11 ...">` で 44px ヒットエリア。`<input type="checkbox">` 直書き禁止、行 checkbox の `aria-label` は意味識別子                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `IntegrationHealthAlert` / `IntegrationHealthAlertClient` / `getIntegrationHealthSummary` | `admin/(dashboard)/_components/IntegrationHealthAlert{.tsx,Client.tsx}` / `@/shared/domain/settings/api-key-queries` | **設定トップ (`/admin/settings`) のみ**で表示する Resend / Stripe / Google Calendar / Cloudflare Turnstile 接続状態 alert（未接続のみリスト表示）。Server (data fetch) + Client (dismiss UI) split。dismiss は未設定 key を sort して `,` 連結した signature を localStorage (`admin:integration-health-alert:dismissed`) に保存し、`useSyncExternalStore` で読む。新たな未設定が増えると signature 不一致で自動再表示。dashboard top には配置しない（ノイズ化防止）。query は 4 統合の `connected: boolean` を一括返却                                                                                                                                                                                                                                                                                                    |
| `UserInfo`                                                                                | `admin/(dashboard)/_components/UserInfo.tsx`                                                                         | サイドバー下部の user 識別 SSoT（avatar + name + email）。desktop / mobile drawer の両方で常時表示。TopBar 右端への email + role 重複表示は撤去済（2 箇所同時表示禁止）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `MediaPickerField`                                                                        | `@/admin/components/media-picker` (`_shared/components/media-picker/MediaPickerField.tsx`)                           | RHF `<FormField>` 配下の単一画像選択 SSoT（favicon / ロゴ / OGP / カバー画像 等）。**UX**: 未選択 = ドロップ枠全体クリック可能 button（アイコン + 推奨サイズ内包、aspectRatio に応じた実比率枠）、選択済 = 実比率プレビュー + 直下に「変更 / 削除」ボタン常時表示（タッチ + キーボード + マウス全環境で同一アクセス、hover overlay は touch 不可のため不採用）。`<FormControl>` Slot 注入の `id` / `aria-describedby` を primary トリガーに forward（`aria-invalid` は button role 非対応のため受け取らず、エラーは `aria-describedby` 経由で FormMessage ID として伝搬）。`alt` required、`aspectRatio: "square" (favicon 1:1) \| "wide" (OGP 1.91:1) \| "logo" (3:1)`、`defaultUsage: MediaUsage`（`SITE` がサイト全体用）。削除は `onChange("")`（`.string().max(500)` + `emptyToNull` 契約）。URL `<Input>` 直書き禁止 |
| `.bg-checker` utility                                                                     | `(admin)/_styles/admin.css` `@layer utilities`                                                                       | 画像プレビュー用チェッカーボード背景の SSoT。透過 PNG / SVG（ロゴ / ファビコン / SVG）の透過部分を市松模様で可視化（業界標準: Sanity Studio / Figma / Photoshop / Adobe XD / Stripe Brand assets）。`linear-gradient` 軽量実装。**使用箇所**: `MediaPickerField` プレビュー / `AutoImageField` 画像 state / `MediaPickerDialog` の `FilePreview` + `UrlTab` プレビュー。**使わない箇所**: グリッドサムネ大量並び（ノイジー）/ 写真専用フィールド（`bg-muted` のままで OK）/ empty state placeholder（灰色のほうが「未設定」と分かる）                                                                                                                                                                                                                                                                                      |

## デザイントークン（@theme）

レスポンシブ @theme tokens は `(public)/_styles/public.css` / `(admin)/_styles/admin.css` の `@theme` ブロックが SSoT。
詳細 token 一覧と利用ガイドは `frontend/project-design-config.md` §レスポンシブ設計 を参照。

主要 token 群:

- Breakpoint: `--breakpoint-3xl: 120rem`
- Header: `--header-height`（mobile-md 分岐）
- Hero/Modal/Lightbox/Dropdown: `--hero-min-height` / `--modal-max-height` / `--lightbox-max-{height,width}` / `--dropdown-min-width`
- Prose/Container: `--prose-{narrow,medium}` / `--container-{measure,header-max,max,padding}` / `--container-editorial`
- Touch target: `--touch-target-min`
- Fluid typography: `--text-*`
- **セクション縦余白（公開・管理の SectionWrapper）**: `--space-3xs` … `--space-2xl` を `pt/pb-[var(--space-{sm,md,lg,xl})]` で参照。旧 `--spacing-section` / `--spacing-section-compact` は **廃止**（`src` では使用禁止、`architecture-boundaries.test.ts` で検出）
- **ブロック・カード用（レイアウトブロック）**: public `--spacing-block` / admin `--spacing-card`（フォーム・カード枠の `py-[var(--spacing-block)]` 等）

### ホームヒーロー（Page 第一級フィールド）

| 定数/変数                          | 場所                                       | メモ                                                         |
| ---------------------------------- | ------------------------------------------ | ------------------------------------------------------------ |
| `pageHeroSchema` / `parsePageHero` | `@/shared/lib/sections/page-hero/schema`   | `Page.pageHero` JSON の正本。管理・公開で共通                |
| `defaultPageHeroHome`              | `@/shared/lib/sections/page-hero/defaults` | seed / 未設定時のホーム既定                                  |
| `<PageHero />`                     | `@/public/components/page-hero/PageHero`   | variant dispatch。旧 `homepage-hero` **Section type は廃止** |

**新規 arbitrary 値（`[65ch]` / `[85vh]` / `[90svh]` / `[12rem]` 等）を追加する前に既存 token を grep し、不足なら `@theme` に追加してから `min-h-[var(--hero-min-height)]` 等の CSS var 参照形式で利用する。**
