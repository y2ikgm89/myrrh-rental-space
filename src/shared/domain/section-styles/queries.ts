import "server-only";

/**
 * Section Style queries — Phase B.P2 stub.
 *
 * Full list / detail / usage implementations land in Phase B.P3 alongside the
 * cascade resolver. This file exists now so server actions can import a
 * stable path, and so the architecture-boundaries test can see the module.
 */

export type SectionStyleQueriesStubMarker = "phase-b-p2";
