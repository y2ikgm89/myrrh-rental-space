import "server-only";

/**
 * Section Style commands — Phase B.P2 stub.
 *
 * Full create / update / delete / derive / detach implementations land in
 * Phase B.P3 alongside the cascade resolver. Keep this module server-only so
 * future additions can rely on `prisma` + `server-only` imports without a
 * reshuffle.
 */

export type SectionStyleCommandsStubMarker = "phase-b-p2";
