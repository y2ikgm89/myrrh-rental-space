/**
 * Lexical 編集 UI 用 Decorator コンポーネント登録（client のみ）。
 * LexicalEditor 読み込み前に import すること。
 */

import "./ImageNode.decorator.client";
import "./InlineImageNode.decorator.client";
import "./PageBreakNode.decorator.client";
import "./AudioNode.decorator.client";
import "./FileNode.decorator.client";
import "./FigmaNode.decorator.client";
import "./SpotifyNode.decorator.client";
import "./ButtonNode.decorator.client";
