# Project-scoped Codex configuration for trusted local work.
# Keep durable team instructions in AGENTS.md and repo skills.

project_doc_max_bytes = 32768

[agents]
max_threads = 4
max_depth = 1
job_max_runtime_seconds = 1800
