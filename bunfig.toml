# Bun設定ファイル
# https://bun.sh/docs/runtime/bunfig

[test]
# テストファイルのプリロード（DOM 環境 + グローバル設定）
preload = ["./__tests__/setup-dom.ts", "./__tests__/setup.ts"]

# E2E テスト（Playwright）を除外
root = "./__tests__"

# カバレッジ設定
coverage = true
coverageReporter = ["text", "lcov"]
coverageDir = "coverage"

# カバレッジ閾値（公式推奨: bunfig.toml で設定）
# 90% — 現状 95%+ でクリア。100% は mock.module カバレッジ計測の Bun 既知制限で不可。
# フォールバック: scripts/check-coverage.mjs（Bun の exit code バグ遭遇時に CI で使用）
coverageThreshold = { line = 0.9, function = 0.9 }

# カバレッジ除外パターン
coverageSkipTestFiles = true
coverageIgnoreSourcemaps = true
coveragePathIgnorePatterns = ["**/__tests__/setup.ts", "**/__tests__/setup-dom.ts"]

# タイムアウト設定（5秒）
timeout = 5000
