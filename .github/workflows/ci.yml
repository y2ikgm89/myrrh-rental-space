name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    # PR に `visual-regression` ラベルが付与されると visual-regression job が起動
    types: [opened, synchronize, reopened, labeled]
  workflow_dispatch:
    inputs:
      run_e2e:
        description: "Run E2E tests"
        type: boolean
        required: false
        default: false
      run_visual:
        description: "Run visual regression tests (with --update-snapshots to regenerate baseline)"
        type: boolean
        required: false
        default: false
      update_visual_baseline:
        description: "Regenerate visual baseline (commit to repo after success)"
        type: boolean
        required: false
        default: false
      run_lighthouse:
        description: "Run Lighthouse CI"
        type: boolean
        required: false
        default: false

# 同一ブランチ・同一イベントタイプで進行中の run があれば cancel。
# event_name を group に含めることで workflow_dispatch (手動 trigger) と push (auto)
# が独立 group となり、互いに cancel し合わない（race condition 回避）。
# main は履歴保持のため cancel しない、feature branch は PR push のたびに前 run を捨てる。
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.event_name }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

env:
  # GitHub-hosted runner の Node デフォルト heap (~2GB) は大規模 monorepo の
  # `tsc --noEmit` や `next build` (TypeScript フェーズ) で不足する。
  # ubuntu-latest は 16 GB RAM 確保されているため 4 GB 割当で確実に賄える。
  NODE_OPTIONS: "--max-old-space-size=4096"
  DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test_db
  # 実 DB 必須の統合テスト（イベント overbooking TOCTOU 等）が接続するテスト DB。
  # 未設定だと当該テストは describe ごと skip される（dev DB 誤汚染防止の安全弁）。
  # unit-tests job が postgres service + `prisma migrate deploy` 済みのため同値を渡す。
  TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test_db
  # server.ts の z.string().min(32) と整合（短い値だと将来 server env を読むテストで破綻する）
  BETTER_AUTH_SECRET: test-secret-key-for-ci-0123456789abcdef0123456789ab
  BETTER_AUTH_URL: http://localhost:3000
  # clientEnv（@t3-oss/env-nextjs）の z.string().url() を満たすため両 URL を渡す。
  # E2E / Visual / Lighthouse の Playwright webServer が `next start` を起動する際に
  # NEXT_PUBLIC_BASE_URL 未設定だと Invalid environment variables で起動失敗する。
  NEXT_PUBLIC_BASE_URL: http://localhost:3000
  NEXT_PUBLIC_APP_URL: http://localhost:3000
  TURNSTILE_SECRET_KEY: 1x0000000000000000000000000000000AA
  NEXT_PUBLIC_TURNSTILE_SITE_KEY: 1x00000000000000000000AA

jobs:
  # ---------------------------------------------------------------------------
  # changes: PR の変更パスを判定し、後続 job の重い step を gate する。
  # docs / rule / コメント-only の PR では full build × 4 が走るのが旧来の最大コスト。
  # required check は job conclusion=success で満たすため、job 自体は常に走らせ、
  # 重い step のみ if: needs.changes.outputs.code で gate する pattern (公式推奨)。
  # ---------------------------------------------------------------------------
  changes:
    name: Detect Code Changes
    runs-on: ubuntu-latest
    outputs:
      code: ${{ steps.filter.outputs.code }}
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.0

      - name: Filter paths
        id: filter
        uses: dorny/paths-filter@fbd0ab8f3e69293af611ebaee6363fc25e6d187d # v4.0.1
        with:
          # base は PR base / main push 時は前 commit (paths-filter default)。
          # ! prefix で「これ以外」を意味し、docs-only PR を skip 対象にする。
          # **/*.md は CLAUDE.md / AGENTS.md / docs/**/*.md / .claude/rules/**/*.md を網羅。
          filters: |
            code:
              - '!**/*.md'
              - '!docs/**'
              - '!.claude/rules/**'
              - '!.claude/agents/**'
              - '!.claude/skills/**'
              - '!.serena/memories/**'
              - '!LICENSE'
              - '!.gitignore'

  # ---------------------------------------------------------------------------
  # migration-safety: 破壊的マイグレーションの後方互換ゲート（Risk 1）。
  # Cloud Run ローリング切替窓（cloudbuild Step6 migrate → Step7 deploy の間も
  # 旧リビジョンが処理）で旧コードが破壊済み新スキーマを叩く 500 を、後方互換でない
  # migration を merge 前にブロックして構造的に防ぐ。規約は .claude/rules/migrations.md。
  # job は常に走らせ（required check の安定化、changes job と同じ公式推奨パターン）、
  # squawk step のみ migration 変更時に gate する。escape hatch は SQL 内の
  # `-- squawk-ignore <rule>`（contract フェーズの意図的 DROP 用）。
  # ---------------------------------------------------------------------------
  migration-safety:
    name: Migration Safety (squawk)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.0

      - name: Detect changed migrations
        id: filter
        uses: dorny/paths-filter@fbd0ab8f3e69293af611ebaee6363fc25e6d187d # v4.0.1
        with:
          # shell: シェルエスケープ済みのファイル一覧を outputs.migrations_files に出力
          list-files: shell
          filters: |
            migrations:
              - 'prisma/migrations/**/migration.sql'

      - name: Setup Bun
        if: steps.filter.outputs.migrations == 'true'
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: package.json

      # 公式リリースの生バイナリを SHA256 検証して直接実行する。
      # npm ラッパー（squawk-cli）は spawn 失敗時に exit 0 を返し偽陰性を生むため不使用。
      # バージョン更新時は SHA256 も更新（gh api .../releases/tags/vX で digest 取得）。
      - name: Download & verify squawk
        if: steps.filter.outputs.migrations == 'true'
        env:
          SQUAWK_VERSION: "2.51.0"
          SQUAWK_SHA256: "8433b7e4500ce6ab7e5b4283ba80be9dde46b7a4de9e22bf766a217777ddb6f9"
        run: |
          curl -fsSL -o squawk \
            "https://github.com/sbdchd/squawk/releases/download/v${SQUAWK_VERSION}/squawk-linux-x64"
          echo "${SQUAWK_SHA256}  squawk" | sha256sum -c -
          chmod +x squawk
          ./squawk --version

      # 毎回 fixture でゲート挙動を実証（unsafe→検出 / safe→通過 / ignored→通過）。
      # squawk の挙動・除外設定の回帰をこの環境で検出する。
      - name: Self-test gate (fixtures)
        if: steps.filter.outputs.migrations == 'true'
        env:
          SQUAWK_BIN: ${{ github.workspace }}/squawk
        run: bun scripts/lint-migrations.ts --selftest

      - name: Lint changed migrations
        if: steps.filter.outputs.migrations == 'true'
        env:
          SQUAWK_BIN: ${{ github.workspace }}/squawk
        run: bun scripts/lint-migrations.ts ${{ steps.filter.outputs.migrations_files }}

  dependency-audit:
    name: Dependency Audit (bun audit)
    runs-on: ubuntu-latest
    needs: [changes]
    steps:
      - name: Checkout
        if: needs.changes.outputs.code == 'true'
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.0

      - name: Setup Bun
        if: needs.changes.outputs.code == 'true'
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          # package.json#packageManager (bun@X.Y.Z) を SSoT として読み取り、
          # workflow への version hardcode drift を防止。
          bun-version-file: package.json

      - name: Cache Bun dependencies
        if: needs.changes.outputs.code == 'true'
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ~/.bun/install/cache
          key: bun-deps-${{ runner.os }}-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            bun-deps-${{ runner.os }}-

      - name: Install dependencies
        if: needs.changes.outputs.code == 'true'
        run: sh scripts/bun-ci-install.sh

      # high 以上の脆弱性検出時は job 失敗。低 severity の transitive deps は許容
      - name: Run bun audit
        if: needs.changes.outputs.code == 'true'
        run: bun audit --prod --severity=high 2>&1 | tee audit-report.txt

      - name: Upload audit report
        if: needs.changes.outputs.code == 'true' && always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: dependency-audit-${{ github.event.pull_request.number || github.sha }}
          path: audit-report.txt
          retention-days: 90

  # ---------------------------------------------------------------------------
  # lint-format / type-check は並列実行。typecheck (~2-3 分) が dominant のため
  # 直列実行時の wall clock 損失を回避。両 job を branch protection の required
  # status check に個別登録する（旧 `Lint & Type Check` wrapper は backwards-compat
  # shim だったため Trunk-Based + Conventional Commits 規律で撤去済み）。
  # ---------------------------------------------------------------------------
  lint-format:
    name: Lint & Format
    runs-on: ubuntu-latest
    needs: [changes]
    steps:
      - name: Checkout
        if: needs.changes.outputs.code == 'true'
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.0

      - name: Setup Bun
        if: needs.changes.outputs.code == 'true'
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: package.json

      - name: Cache Bun dependencies
        if: needs.changes.outputs.code == 'true'
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ~/.bun/install/cache
          key: bun-deps-${{ runner.os }}-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            bun-deps-${{ runner.os }}-

      - name: Install dependencies
        if: needs.changes.outputs.code == 'true'
        run: sh scripts/bun-ci-install.sh

      # Prettier format check を全ファイル対象で実行（lefthook が pre-commit で
      # auto-fix する前提だが、CI でも最終ゲートとして検証）
      - name: Check code formatting
        if: needs.changes.outputs.code == 'true'
        run: bun run format:check

      - name: Run lint
        if: needs.changes.outputs.code == 'true'
        run: bun run lint

  type-check:
    name: Type Check
    runs-on: ubuntu-latest
    needs: [changes]
    steps:
      - name: Checkout
        if: needs.changes.outputs.code == 'true'
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.0

      - name: Setup Bun
        if: needs.changes.outputs.code == 'true'
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: package.json

      - name: Cache Bun dependencies
        if: needs.changes.outputs.code == 'true'
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ~/.bun/install/cache
          key: bun-deps-${{ runner.os }}-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            bun-deps-${{ runner.os }}-

      - name: Install dependencies
        if: needs.changes.outputs.code == 'true'
        run: sh scripts/bun-ci-install.sh

      - name: Generate Prisma client
        if: needs.changes.outputs.code == 'true'
        run: bunx --bun prisma generate

      - name: Run type check
        if: needs.changes.outputs.code == 'true'
        run: bun run type-check

  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    needs: [changes]
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout
        if: needs.changes.outputs.code == 'true'
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.0

      - name: Setup Bun
        if: needs.changes.outputs.code == 'true'
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: package.json

      - name: Cache Bun dependencies
        if: needs.changes.outputs.code == 'true'
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ~/.bun/install/cache
          key: bun-deps-${{ runner.os }}-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            bun-deps-${{ runner.os }}-

      - name: Install dependencies
        if: needs.changes.outputs.code == 'true'
        run: sh scripts/bun-ci-install.sh

      - name: Generate Prisma client
        if: needs.changes.outputs.code == 'true'
        run: bunx --bun prisma generate

      - name: Run database migrations
        if: needs.changes.outputs.code == 'true'
        run: bunx --bun prisma migrate deploy

      # per-file isolation 実行（scripts/run-tests.ts）必須:
      # mock.module() は process グローバルに live binding を残すため、複数 test
      # ファイルを同一 bun test 起動で実行すると干渉する（公式 docs の guidance）。
      # 各 *.test.ts を独立 bun サブプロセスで起動して干渉を物理的に排除する。
      - name: Run unit tests (per-file isolation)
        if: needs.changes.outputs.code == 'true'
        run: bun run test:unit

      - name: Run integration tests (per-file isolation)
        if: needs.changes.outputs.code == 'true'
        run: bun run test:integration

  smoke-e2e:
    name: Smoke E2E (critical path)
    runs-on: ubuntu-latest
    needs: [changes]
    # 毎 push 実行する critical path smoke。`chromium-smoke` project
    # (`e2e/smoke/*.smoke.spec.ts`) のみ実行。10 test / 3 分以内完走を SLA とする。
    # 業界標準: Stripe / Vercel / Linear / Shopify の "fast PR feedback smoke" pattern。
    # 広域 E2E (`e2e-tests` job) は label opt-in 維持、本 job のみ
    # branch protection required status checks に含める。
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout
        if: needs.changes.outputs.code == 'true'
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.0

      - name: Setup Bun
        if: needs.changes.outputs.code == 'true'
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: package.json

      - name: Cache Bun dependencies
        if: needs.changes.outputs.code == 'true'
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ~/.bun/install/cache
          key: bun-deps-${{ runner.os }}-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            bun-deps-${{ runner.os }}-

      - name: Install dependencies
        if: needs.changes.outputs.code == 'true'
        run: sh scripts/bun-ci-install.sh

      - name: Cache Playwright browsers
        if: needs.changes.outputs.code == 'true'
        id: playwright-cache
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            playwright-${{ runner.os }}-

      - name: Install Playwright browsers
        if: needs.changes.outputs.code == 'true' && steps.playwright-cache.outputs.cache-hit != 'true'
        run: bunx playwright install chromium --with-deps

      - name: Install Playwright system deps (cache hit path)
        if: needs.changes.outputs.code == 'true' && steps.playwright-cache.outputs.cache-hit == 'true'
        run: bunx playwright install-deps chromium

      - name: Generate Prisma client
        if: needs.changes.outputs.code == 'true'
        run: bunx --bun prisma generate

      - name: Run database migrations
        if: needs.changes.outputs.code == 'true'
        run: bunx --bun prisma migrate deploy

      - name: Seed test database
        if: needs.changes.outputs.code == 'true'
        run: bunx --bun prisma db seed

      - name: Build application
        if: needs.changes.outputs.code == 'true'
        run: bun run build:skip-env
        env:
          NEXT_PUBLIC_ENABLE_E2E_LOGIN: "1"
          SKIP_ENV_VALIDATION: "true"

      - name: Run smoke E2E tests
        if: needs.changes.outputs.code == 'true'
        run: bunx playwright test --project=chromium-smoke
        env:
          CI: true
          PLAYWRIGHT_BASE_URL: http://localhost:3000
          NEXT_PUBLIC_ENABLE_E2E_LOGIN: "1"
          ENCRYPTION_KEY: "0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"
          CRON_SECRET: "test-cron-secret-for-ci-smoke-strict-0123456789ab"
          ADMIN_LOGIN_TOKEN: "test-admin-login-token-for-ci-smoke-strict-0123456789ab"
          R2_ACCOUNT_ID: "test-r2-account"
          R2_ACCESS_KEY_ID: "test-r2-access-key"
          R2_SECRET_ACCESS_KEY: "test-r2-secret-key-32-min-length!!"
          R2_BUCKET_NAME: "test-r2-bucket"
          R2_PUBLIC_URL: "https://example.com"
          RESEND_API_KEY: "re_test_placeholder_for_ci_smoke_only"
          STRIPE_SECRET_KEY: "sk_test_placeholder_for_ci_smoke_only"
          GOOGLE_CLIENT_ID: "test-google-client-id.apps.googleusercontent.com"
          GOOGLE_CLIENT_SECRET: "test-google-client-secret-for-ci-smoke"

      - name: Upload smoke Playwright report
        if: needs.changes.outputs.code == 'true' && always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: playwright-smoke-report-${{ github.event.pull_request.number || github.sha }}
          path: playwright-report/
          retention-days: 14

  e2e-tests:
    name: E2E Tests
    runs-on: ubuntu-latest
    # 広域 E2E は重く maintenance コストが高いため opt-in 実行（Stripe / Vercel / Linear
    # 等の業界標準パターン）。critical path は `smoke-e2e` job が毎 push でカバー。
    # - PR に `e2e` ラベル付与時 (labeled イベント含む)
    # - workflow_dispatch で手動実行時
    # main への push では実行しない。本番デプロイ前検証は
    # post-deploy-smoke skill (HTTP smoke) と Cloud Run staging で代替。
    if: |
      (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'e2e')) ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.run_e2e == 'true')
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.0

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: package.json

      - name: Cache Bun dependencies
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ~/.bun/install/cache
          key: bun-deps-${{ runner.os }}-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            bun-deps-${{ runner.os }}-

      - name: Install dependencies
        run: sh scripts/bun-ci-install.sh

      # Playwright browsers は大きい (chromium ~130MB)。hash ベースで cache
      - name: Cache Playwright browsers
        id: playwright-cache
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            playwright-${{ runner.os }}-

      - name: Install Playwright browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: bunx playwright install chromium --with-deps

      - name: Install Playwright system deps (cache hit path)
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        run: bunx playwright install-deps chromium

      - name: Generate Prisma client
        run: bunx --bun prisma generate

      - name: Run database migrations
        run: bunx --bun prisma migrate deploy

      - name: Seed test database
        run: bunx --bun prisma db seed

      - name: Build application
        run: bun run build:skip-env
        env:
          # NEXT_PUBLIC_* は build 時に static replace されるため build step で渡す。
          # E2E 専用 opt-in: DevLoginButton を production build でも表示し、
          # setup-customer / setup-admin が dev login ボタンをクリックできるようにする。
          # staging / production には絶対伝播させない（login bypass risk）。
          NEXT_PUBLIC_ENABLE_E2E_LOGIN: "1"
          SKIP_ENV_VALIDATION: "true"

      - name: Run E2E tests
        run: bunx playwright test
        env:
          CI: true
          PLAYWRIGHT_BASE_URL: http://localhost:3000
          # webServer が next start で起動する際の runtime env。
          NEXT_PUBLIC_ENABLE_E2E_LOGIN: "1"
          # production build を `next start` で動かすため validateProductionEnv の
          # 要求 env を埋める（lhci-start.ts と同方針）。
          ENCRYPTION_KEY: "0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"
          CRON_SECRET: "test-cron-secret-for-ci-e2e-strict-0123456789ab"
          ADMIN_LOGIN_TOKEN: "test-admin-login-token-for-ci-e2e-strict-0123456789ab"
          R2_ACCOUNT_ID: "test-r2-account"
          R2_ACCESS_KEY_ID: "test-r2-access-key"
          R2_SECRET_ACCESS_KEY: "test-r2-secret-key-32-min-length!!"
          R2_BUCKET_NAME: "test-r2-bucket"
          R2_PUBLIC_URL: "https://example.com"
          RESEND_API_KEY: "re_test_placeholder_for_ci_e2e_only"
          STRIPE_SECRET_KEY: "sk_test_placeholder_for_ci_e2e_only"
          GOOGLE_CLIENT_ID: "test-google-client-id.apps.googleusercontent.com"
          GOOGLE_CLIENT_SECRET: "test-google-client-secret-for-ci-e2e"

      - name: Upload Playwright report
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

  build:
    name: Build (env validation)
    runs-on: ubuntu-latest
    needs: [changes, lint-format, type-check]
    if: |
      always() && needs.lint-format.result == 'success' && needs.type-check.result == 'success' &&
      (github.event_name == 'pull_request' ||
       (github.event_name == 'push' && github.ref == 'refs/heads/main'))
    steps:
      - name: Checkout
        if: needs.changes.outputs.code == 'true'
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.0

      - name: Setup Bun
        if: needs.changes.outputs.code == 'true'
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: package.json

      - name: Cache Bun dependencies
        if: needs.changes.outputs.code == 'true'
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ~/.bun/install/cache
          key: bun-deps-${{ runner.os }}-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            bun-deps-${{ runner.os }}-

      - name: Install dependencies
        if: needs.changes.outputs.code == 'true'
        run: sh scripts/bun-ci-install.sh

      - name: Generate Prisma client
        if: needs.changes.outputs.code == 'true'
        run: bunx --bun prisma generate

      - name: Build with env validation
        if: needs.changes.outputs.code == 'true'
        run: bun run build
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test_db
          BETTER_AUTH_SECRET: 0123456789abcdef0123456789abcdef0123456789abcdef0123456789ab
          BETTER_AUTH_URL: http://localhost:3000
          NEXT_PUBLIC_APP_URL: http://localhost:3000
          NEXT_PUBLIC_BASE_URL: http://localhost:3000
          TURNSTILE_SECRET_KEY: 1x0000000000000000000000000000000AA
          NEXT_PUBLIC_TURNSTILE_SITE_KEY: 1x0000000000000000000000000000000AA
          NEXT_PUBLIC_GA_MEASUREMENT_ID: G-XXXXXXXXXX
          ENCRYPTION_KEY: 0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
          CRON_SECRET: test-cron-secret-for-ci-build-strict-0123456789ab
          ADMIN_LOGIN_TOKEN: test-admin-login-token-for-ci-build-strict-0123456789ab
          RESEND_API_KEY: re_test_placeholder_for_ci_strict_build_only
          STRIPE_SECRET_KEY: sk_test_placeholder_for_ci_strict_build_only
          GOOGLE_CLIENT_ID: test-google-client-id.apps.googleusercontent.com
          GOOGLE_CLIENT_SECRET: test-google-client-secret-for-ci-strict-build

  bundle-size-diff:
    name: Bundle Size Diff (PR comment)
    runs-on: ubuntu-latest
    needs: [changes, lint-format, type-check]
    # PR のみ実行（base vs head の gzipped size 差分を sticky comment）。
    # docs/rule-only PR は changes.code=false で skip (bundle 影響なしのため正)。
    if: |
      always() && needs.lint-format.result == 'success' && needs.type-check.result == 'success' &&
      github.event_name == 'pull_request' &&
      needs.changes.outputs.code == 'true'
    permissions:
      pull-requests: write
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.0

      # preactjs/compressed-size-action は内部で `bun install --frozen-lockfile`
      # （= `bun ci` の同義 alias、Bun 公式 docs）を呼ぶため、setup-bun を先に実行
      # して PATH に bun を通す必要がある
      # （未設定だと "Unable to locate executable file: bun" で fail）
      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: package.json

      - name: Cache Bun dependencies
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ~/.bun/install/cache
          key: bun-deps-${{ runner.os }}-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            bun-deps-${{ runner.os }}-

      # preactjs/compressed-size-action:
      # base と head で build を実行し、指定 pattern のファイルサイズを gzip で比較
      # 参照: https://github.com/preactjs/compressed-size-action
      - name: Compressed size diff
        uses: preactjs/compressed-size-action@v2
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          build-script: "build:skip-env"
          pattern: ".next/static/**/*.{js,css}"
          exclude: "{**/*.map,**/node_modules/**}"
          # 1KB 以下の増減は警告しない（noise 削減）
          minimum-change-threshold: 1024

  bundle-analysis:
    name: Bundle Analysis (Turbopack)
    runs-on: ubuntu-latest
    needs: [lint-format, type-check]
    # main push のみ実行（PR は bundle-size-diff で代替、build 重複回避）。
    # PR の bundle 影響可視化は bundle-size-diff の sticky comment で十分、
    # archived analysis は historical trend 用のため main のみで artifact 保管。
    if: |
      always() && needs.lint-format.result == 'success' && needs.type-check.result == 'success' &&
      github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.0

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: package.json

      - name: Cache Bun dependencies
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ~/.bun/install/cache
          key: bun-deps-${{ runner.os }}-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            bun-deps-${{ runner.os }}-

      - name: Install dependencies
        run: sh scripts/bun-ci-install.sh

      - name: Generate Prisma client
        run: bunx --bun prisma generate

      # Next.js 16 公式の Turbopack-native bundle analyzer
      # 結果は .next/diagnostics/analyze に出力される
      - name: Run bundle analyzer
        run: bun run analyze
        env:
          SKIP_ENV_VALIDATION: "true"

      - name: Upload bundle analysis artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: bundle-analysis-${{ github.event.pull_request.number || github.sha }}
          path: .next/diagnostics/analyze/
          retention-days: 30

  lighthouse-ci:
    name: Lighthouse CI (perf / a11y / SEO)
    runs-on: ubuntu-latest
    needs: [lint-format, type-check]
    # Lighthouse は重く Cloud Run staging URL での実測の方が本番に近い指標が得られる
    # ため opt-in 実行に変更（Web.dev / Calibre 等の現代パフォーマンス計測のベスト
    # プラクティス: 静的 build を runner で測るより staging 環境の RUM ベース計測を
    # 優先）。
    # - PR に `lighthouse` ラベル付与時
    # - workflow_dispatch で手動実行時
    if: |
      always() && needs.lint-format.result == 'success' && needs.type-check.result == 'success' &&
      ((github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'lighthouse')) ||
       (github.event_name == 'workflow_dispatch' && github.event.inputs.run_lighthouse == 'true'))
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.0

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: package.json

      - name: Cache Bun dependencies
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ~/.bun/install/cache
          key: bun-deps-${{ runner.os }}-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            bun-deps-${{ runner.os }}-

      - name: Install dependencies
        run: sh scripts/bun-ci-install.sh

      - name: Generate Prisma client
        run: bunx --bun prisma generate

      - name: Run database migrations
        run: bunx --bun prisma migrate deploy

      - name: Seed test database
        run: bunx --bun prisma db seed

      # Lighthouse CI autorun: .lighthouserc.json 設定に従い
      # collect → assert → upload (temporary-public-storage) を一括実行
      - name: Run Lighthouse CI
        run: bunx lhci autorun --config=.lighthouserc.json
        env:
          LHCI_GITHUB_APP_TOKEN: ${{ secrets.LHCI_GITHUB_APP_TOKEN }}

      - name: Upload Lighthouse report artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: lighthouse-report-${{ github.event.pull_request.number || github.sha }}
          path: .lighthouseci/
          retention-days: 30

  visual-regression:
    name: Visual Regression (opt-in)
    runs-on: ubuntu-latest
    # opt-in 実行条件:
    # - PR に `visual-regression` ラベルが付与された（labeled イベント含む）
    # - workflow_dispatch で run_visual=true 指定（baseline 比較）
    # - workflow_dispatch で update_visual_baseline=true 指定（baseline 再生成 + auto-PR）
    # baseline regeneration は意図的な workflow_dispatch でのみ実行する（main push
    # 自動 regeneration は破壊的変更時に baseline を silent 上書きする risk のため廃止）。
    # 再生成 baseline は main へ直接 push せず別 branch + auto-PR を作成し、
    # branch protection の required status checks を通過させてから merge する
    # （Playwright / Chromatic / Percy 業界標準パターン）。
    permissions:
      contents: write # auto-PR 用 branch push
      pull-requests: write # auto-PR 作成
    if: |
      (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'visual-regression')) ||
      (github.event_name == 'workflow_dispatch' && (github.event.inputs.run_visual == 'true' || github.event.inputs.update_visual_baseline == 'true'))
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.0

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: package.json

      - name: Cache Bun dependencies
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ~/.bun/install/cache
          key: bun-deps-${{ runner.os }}-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            bun-deps-${{ runner.os }}-

      - name: Install dependencies
        run: sh scripts/bun-ci-install.sh

      - name: Install Playwright browsers
        run: bunx playwright install chromium --with-deps

      - name: Generate Prisma client
        run: bunx --bun prisma generate

      - name: Run database migrations
        run: bunx --bun prisma migrate deploy

      - name: Seed test database
        run: bunx --bun prisma db seed

      - name: Build application
        run: bun run build:skip-env
        env:
          SKIP_ENV_VALIDATION: "true"

      # Playwright 公式の visual regression:
      # - baseline が存在すれば diff 検証
      # - update_visual_baseline=true で --update-snapshots を渡して baseline を再生成
      #   （CI Ubuntu runner と本番の font rendering が一致するため CI 上で baseline 生成が canonical）
      - name: Run visual regression tests
        run: |
          if [ "${{ github.event.inputs.update_visual_baseline }}" = "true" ]; then
            bunx playwright test --project=chromium-visual --update-snapshots
          else
            bunx playwright test --project=chromium-visual
          fi
        env:
          CI: true
          PLAYWRIGHT_BASE_URL: http://localhost:3000
          PLAYWRIGHT_VISUAL: "1"
          # production build を `next start` で動かすため validateProductionEnv の
          # 要求 env を埋める（webServer は playwright.config.ts の分岐により CI で next start）。
          ENCRYPTION_KEY: "0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"
          CRON_SECRET: "test-cron-secret-for-ci-visual-strict-0123456789ab"
          ADMIN_LOGIN_TOKEN: "test-admin-login-token-for-ci-visual-strict-0123456789ab"
          R2_ACCOUNT_ID: "test-r2-account"
          R2_ACCESS_KEY_ID: "test-r2-access-key"
          R2_SECRET_ACCESS_KEY: "test-r2-secret-key-32-min-length!!"
          R2_BUCKET_NAME: "test-r2-bucket"
          R2_PUBLIC_URL: "https://example.com"
          RESEND_API_KEY: "re_test_placeholder_for_ci_visual_only"
          STRIPE_SECRET_KEY: "sk_test_placeholder_for_ci_visual_only"
          GOOGLE_CLIENT_ID: "test-google-client-id.apps.googleusercontent.com"
          GOOGLE_CLIENT_SECRET: "test-google-client-secret-for-ci-visual"

      - name: Upload Playwright visual diff report
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: playwright-visual-report-${{ github.event.pull_request.number || github.sha }}
          path: |
            playwright-report/
            e2e/visual/**/*-snapshots/
          retention-days: 30

      # baseline 再生成成功時、生成された snapshot を別 branch にステージし PR を作成する。
      # 業界標準パターン（Playwright docs / Chromatic / Percy / peter-evans/create-pull-request
      # は GitHub Marketplace verified action）。Branch protection の required status checks
      # を通過させてから main に merge することで「未検証 baseline の silent 上書き」を防ぐ。
      - name: Stage regenerated visual baseline
        if: github.event_name == 'workflow_dispatch' && github.event.inputs.update_visual_baseline == 'true' && success()
        id: stage-baseline
        shell: bash
        run: |
          shopt -s globstar nullglob
          # bash globstar で **/*-snapshots/ をディレクトリ展開（null glob で 0 件でも fail しない）
          baseline_dirs=(e2e/visual/**/*-snapshots/)
          if [ ${#baseline_dirs[@]} -eq 0 ]; then
            echo "[visual baseline] no -snapshots/ directories found"
            echo "has_changes=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          # git add 対象を明示。peter-evans/create-pull-request@v6 が diff 検出 + commit + push を担当
          git add "${baseline_dirs[@]}"
          if git diff --cached --quiet; then
            echo "[visual baseline] no changes to commit"
            echo "has_changes=false" >> "$GITHUB_OUTPUT"
          else
            echo "[visual baseline] changes detected, will create PR"
            echo "has_changes=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Create PR for regenerated baseline
        if: steps.stage-baseline.outputs.has_changes == 'true'
        uses: peter-evans/create-pull-request@5f6978faf089d4d20b00c7766989d076bb2fc7f1 # v8.1.1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: ci/visual-baseline-${{ github.run_id }}
          base: ${{ github.ref_name }}
          commit-message: "ci(visual): regenerate visual baseline (run ${{ github.run_id }})"
          title: "ci(visual): regenerate visual baseline"
          body: |
            ## Visual baseline regeneration

            **Trigger**: workflow_dispatch (`update_visual_baseline=true`)
            **Actor**: @${{ github.actor }}
            **Run**: [#${{ github.run_id }}](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})
            **Base branch**: `${{ github.ref_name }}`

            ## Review checklist

            - [ ] `e2e/visual/**/*-snapshots/` の diff が **意図したデザイン変更** に対応している
            - [ ] CI Ubuntu runner の font rendering 起因の noise diff ではない
            - [ ] Required status checks 全て green
            - [ ] 関連する production code 変更が main に merge 済み（baseline 先行は禁止）

            ## Auto-generated note

            この PR は CI が自動生成しました。Playwright `--update-snapshots` で再生成された baseline を
            branch protection の required status checks 経由で merge するため、main に直接 push せず PR 化しています。

            🤖 Generated with [peter-evans/create-pull-request@v6](https://github.com/peter-evans/create-pull-request)
          labels: |
            visual-baseline
            automated
          author: "github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>"
          committer: "github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>"
          delete-branch: true

  docs:
    name: Generate API Docs
    runs-on: ubuntu-latest
    needs: [lint-format, type-check]
    if: |
      always() && needs.lint-format.result == 'success' && needs.type-check.result == 'success' &&
      github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.0

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: package.json

      - name: Cache Bun dependencies
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ~/.bun/install/cache
          key: bun-deps-${{ runner.os }}-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            bun-deps-${{ runner.os }}-

      - name: Install dependencies
        run: sh scripts/bun-ci-install.sh

      - name: Generate API documentation
        run: bun run docs

      - name: Upload API docs artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: api-docs
          path: docs/api/
          retention-days: 90
