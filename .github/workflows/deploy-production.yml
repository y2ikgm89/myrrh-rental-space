name: Deploy Production

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-production
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write

env:
  PROJECT_ID: myrrh-rental-space
  PROJECT_NUMBER: "626108938746"
  REGION: asia-northeast1
  SERVICE_NAME: myrrh-rental-space
  ADMIN_SERVICE_NAME: myrrh-rental-space-admin
  AR_REPOSITORY: myrrh-rental-space
  WORKER_POOL: myrrh-deploy-pool
  RUNTIME_SERVICE_ACCOUNT: myrrh-rental-space-runtime@myrrh-rental-space.iam.gserviceaccount.com
  BUILD_SERVICE_ACCOUNT: myrrh-rental-space-build@myrrh-rental-space.iam.gserviceaccount.com
  CRON_SERVICE_ACCOUNT_EMAIL: myrrh-rental-space-scheduler@myrrh-rental-space.iam.gserviceaccount.com
  CRON_OIDC_AUDIENCE: https://rental-space.myrrh-jp.com
  WORKLOAD_IDENTITY_PROVIDER: projects/626108938746/locations/global/workloadIdentityPools/github-actions/providers/github-myrrh-rental-space
  PUBLIC_DOMAIN: https://rental-space.myrrh-jp.com
  ADMIN_DOMAIN: https://admin.myrrh-jp.com
  IAP_JWT_AUDIENCE: /projects/626108938746/locations/asia-northeast1/services/myrrh-rental-space-admin
  ADMIN_ROLE_GROUP_SUPER_ADMIN_EMAIL: myrrh-super-admins@myrrh-jp.com
  ADMIN_ROLE_GROUP_ADMIN_EMAIL: myrrh-admins@myrrh-jp.com
  ADMIN_ROLE_GROUP_EDITOR_EMAIL: myrrh-editors@myrrh-jp.com
  ADMIN_ROLE_GROUP_VIEWER_EMAIL: myrrh-viewers@myrrh-jp.com
  NEXT_PUBLIC_TURNSTILE_SITE_KEY: 0x4AAAAAADi6Bqavj97fu7JG

jobs:
  # Terraform apply を deploy より先に実行する (Codex P1 F6)。
  #
  # 過去は terraform.yml (push-to-main で terraform/** 変更に反応する path
  # フィルタ付き) と本 workflow が push-to-main で並列トリガーされ、両者の
  # concurrency group が別 (terraform-* / deploy-production) だったため実行
  # 順序が保証されていなかった。secret_iam の binding が Cloud Run deploy
  # より遅く反映されると `--set-secrets=` が Permission denied で silent fail
  # する race があった。
  #
  # 解決: terraform apply を deploy の needs 前段として本 workflow に統合し、
  # terraform.yml 側の push トリガー / apply job は廃止 (PR-only validate 化)。
  # terraform apply は idempotent なので、terraform 差分ゼロでも数秒で終わる。
  #
  # ⚠️ 2026-07-14 一時措置 (production deploy unblock):
  #   Terraform state が空 (既存 GCP resources 未 import) + runner IAM が不完全
  #   (bootstrap 未完了) のため terraform apply が create attempt で 409/403 連発
  #   → 全 main push で deploy が blocked していた。段階復旧のため:
  #
  #     - `continue-on-error: true` で apply 失敗が deploy を blocking しない
  #     - deploy job は needs: terraform-apply を維持 (race 順序は保つ)
  #
  #   段階 1 (PR #1075、この commit): continue-on-error を投入して production
  #     deploy を復旧。次 main push で通常 deploy が走る (terraform apply の
  #     成否に関係なく)。
  #
  #   段階 2 (PR #1076): pre-existing GCP resources を Terraform 1.7+
  #     `import{}` blocks で state に宣言的に import → fresh-state apply が
  #     idempotent no-op になり 409 "already exists" を根本的に解消。
  #     terraform/*.tf の各 resource に対応する import block が同一 file に
  #     共存し、architecture-boundaries.test.ts の drift-gate で欠落を強制検知。
  #
  #   段階 3 (未実施、user 手作業): user が `bash scripts/bootstrap-terraform.sh`
  #     を local から再実行し、runner IAM (run.admin / compute.networkAdmin /
  #     compute.securityAdmin / iap.admin / serviceAccountUser on runtime-sa
  #     +scheduler-sa + serviceusage.serviceUsageAdmin) を完成させる。
  #
  #   段階 3 完了後 → terraform apply が真に idempotent no-op → 別 PR で本
  #   continue-on-error を削除して strict blocking gate に戻す (Codex P1 F6 の
  #   secret race protect を復元)。
  #
  # 2026-07-14 段階 4 完了 (このPR): terraform apply が commit adb54eaf6 (PR #1079
  # merge) で完全 success を確認 (import 44 + add 6 + change 7 + destroy 0 全て
  # 適用済み)。continue-on-error を削除して strict blocking gate 復元 →
  # terraform apply の失敗が deploy job を blocking する Codex P1 F6 race protect
  # 復活。以降は「code に問題がある PR は main に到達しない (PR gate) + main の
  # apply が失敗したら deploy も止まる (この strict gate)」の 2 層防御。
  terraform-apply:
    name: Terraform Apply (IAM prereq)
    if: github.repository == 'y2ikgm89/myrrh-rental-space' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      id-token: write
      # 失敗時に GitHub Issue を auto-open するため (最後の step 参照)。
      # PR #1097-#1101 の 4 連続 apply failure が silent に累積して user 検知
      # まで ~2 時間 lag した saga への対策。
      issues: write
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        with:
          workload_identity_provider: ${{ env.WORKLOAD_IDENTITY_PROVIDER }}
          service_account: terraform-runner@myrrh-rental-space.iam.gserviceaccount.com
          project_id: ${{ env.PROJECT_ID }}

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@b9cd54a3c349d3f38e8881555d616ced269862dd # v3.1.2
        with:
          terraform_version: 1.10.0

      - name: Terraform init
        working-directory: terraform
        run: terraform init -input=false
        env:
          TF_IN_AUTOMATION: "1"
          # Cloudflare provider v5 は `CLOUDFLARE_API_TOKEN` env var を
          # 自動採用する (terraform/cloudflare_provider.tf 参照)。init step
          # 自体は provider 認証を要さないが、provider fetch キャッシュに
          # 触れる potential path として先行注入する。Terraform 専用 token
          # (Zone.DNS/Zone.Cache Rules/... Write scope) は runtime token
          # (Cache Purge only) と発行/rotation を分離してある。
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_TERRAFORM_API_TOKEN }}

      # 2026-07-14 Sprint 1 Item F: plan → artifact → summary → apply.
      #
      # 過去は `terraform apply -auto-approve` のみで、diff が Actions log
      # (default 90d retention) にしか残らなかった。post-mortem 時に
      # 「Tuesday 03:14 UTC に何が変わったか」を確認するには、log を retention
      # 内に取得するか、当該 SHA 前後の git bisect で再構築する必要があった。
      #
      # 段階 1-4 unblock saga (PR #1075/1076/1078/1079/1081) で確立した監査 loop
      # (drift detection + strict blocking gate) を完成させるため、apply 前に
      # binary plan + 人間可読 text diff の両方を artifact として保存し、
      # Actions run 画面の Summary tab で即読める形にする。
      #
      # 契約:
      #   - artifact 名は `terraform-plan-${{ github.sha }}` で commit 固定
      #   - retention は GitHub default (90d) — repo settings 変更不要
      #   - plan は idempotent (段階 4 で apply 完全 success 確認済み) なので、
      #     no-op run でも監査 record が残る
      #   - plan 出力に secret 値は含まれない設計 (secrets.tf は container 管理
      #     のみ・値は project owner が gcloud で手動投入) だが、grep gate で
      #     literal value 混入を fail-fast で防ぐ
      - name: Terraform plan
        id: plan
        working-directory: terraform
        shell: bash
        run: |
          set +e
          terraform plan -out=tfplan -input=false -detailed-exitcode -no-color
          EXITCODE=$?
          set -e
          # -detailed-exitcode: 0=no changes, 1=error, 2=changes present
          if [ "$EXITCODE" -eq 1 ]; then
            echo "::error::terraform plan failed" >&2
            exit 1
          fi
          echo "exitcode=$EXITCODE" >> "$GITHUB_OUTPUT"
          if [ "$EXITCODE" -eq 2 ]; then
            echo "Plan produced changes (exit 2) — proceeding to apply"
          else
            echo "Plan is a no-op (exit 0)"
          fi
        env:
          TF_IN_AUTOMATION: "1"
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_TERRAFORM_API_TOKEN }}
          # cloudflare_ruleset.transform_rules_late.value を Cloudflare へ書き込む
          # sensitive TF_VAR (variables.tf cloudflare_origin_header_secret 参照)。
          # rate-limit trust chain の共有 secret、Cloud Run 側 Secret Manager
          # `CLOUDFLARE_ORIGIN_HEADER_SECRET` と同一値を維持する契約。
          TF_VAR_cloudflare_origin_header_secret: ${{ secrets.CLOUDFLARE_ORIGIN_HEADER_SECRET_TF }}

      - name: Render plan to text
        working-directory: terraform
        shell: bash
        run: terraform show -no-color tfplan > tfplan.txt

      - name: Verify plan does not leak secret values
        working-directory: terraform
        shell: bash
        run: |
          # secrets.tf 設計上、Terraform は secret container のみ管理し
          # 値 (google_secret_manager_secret_version.secret_data 等) は
          # project owner が gcloud で手動投入する契約 (Phase 3 SSoT)。
          #
          # 念のため plan output に literal secret 値が誤って混入していない
          # ことを gate する。対象 attribute 名 (secret_id / password_policy /
          # google_secret_manager_secret 等) の単純出現は無視し、値付き代入
          # (`attr = "..."` / diff の `+ attr = "..."`) のみ検知:
          if grep -Ein '^[[:space:]]*[+-]?[[:space:]]*(secret_data|password|private_key)[[:space:]]*=[[:space:]]*"[^"]+"' tfplan.txt; then
            echo "::error::Plan appears to contain literal secret values — investigate the hits above before merging" >&2
            exit 1
          fi
          echo "Secret-leak grep gate: OK"

      - name: Upload plan artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: terraform-plan-${{ github.sha }}
          path: |
            terraform/tfplan
            terraform/tfplan.txt
          retention-days: 90
          if-no-files-found: error

      - name: Append plan to job summary
        working-directory: terraform
        shell: bash
        run: |
          {
            echo "## Terraform plan"
            echo ""
            echo "- Commit: \`${GITHUB_SHA}\`"
            echo "- Plan exit code: \`${{ steps.plan.outputs.exitcode }}\` (0=no changes, 2=changes present)"
            echo "- Artifact: \`terraform-plan-${GITHUB_SHA}\` (retention 90d)"
            echo ""
            echo '```hcl'
            cat tfplan.txt
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Terraform apply
        working-directory: terraform
        run: terraform apply -input=false tfplan
        env:
          TF_IN_AUTOMATION: "1"
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_TERRAFORM_API_TOKEN }}
          # cloudflare_ruleset.transform_rules_late.value を Cloudflare へ書き込む
          # sensitive TF_VAR (variables.tf cloudflare_origin_header_secret 参照)。
          # rate-limit trust chain の共有 secret、Cloud Run 側 Secret Manager
          # `CLOUDFLARE_ORIGIN_HEADER_SECRET` と同一値を維持する契約。
          TF_VAR_cloudflare_origin_header_secret: ${{ secrets.CLOUDFLARE_ORIGIN_HEADER_SECRET_TF }}

      # ---- Failure notification (2026-07-14 追加、apply silent failure 対策) ----
      #
      # terraform-plan / terraform-apply が失敗した場合、GitHub Issue を auto-open。
      # `terraform-drift.yml` の drift 検知 issue と同じ pattern (label /
      # body format / gh CLI で一貫させる)。
      #
      # 背景: PR #1097-#1101 で 4 連続の terraform-apply 失敗 (Cloudflare v5 ruleset
      # 導入時の import ID 不備) が silent に累積、user 検知まで ~2 時間 lag。
      # auto-merge が required PR check のみ見るため post-merge failure が可視化
      # されない構造的問題。この step + `Main Terraform Health` gate
      # (`.github/workflows/check-main-terraform-health.yml`) の 2 層で救済。
      #
      # 重複 issue 防止: `deploy-broken` label + 同一 SHA の未 close issue が
      # あれば skip (idempotent)。
      - name: Open apply failure issue
        # 前段 step (Terraform plan / Terraform apply) いずれかが失敗した時のみ発火。
        # `${{ failure() }}` は job 内で以前の step が failed かをチェックする。
        if: ${{ failure() }}
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          # Idempotent label create
          gh label create deploy-broken \
            --repo "${GITHUB_REPOSITORY}" \
            --description "Post-merge terraform-apply or deploy step failure on main" \
            --color "b60205" 2>/dev/null || true

          # 同 SHA の未 close issue が既にあれば skip
          EXISTING="$(gh issue list \
            --repo "${GITHUB_REPOSITORY}" \
            --label deploy-broken \
            --state open \
            --json title \
            --jq '.[] | select(.title | contains("'"${GITHUB_SHA:0:8}"'")) | .title' \
            | head -1 || true)"
          if [ -n "${EXISTING}" ]; then
            echo "Existing open issue for SHA ${GITHUB_SHA:0:8} — skip (${EXISTING})"
            exit 0
          fi

          TODAY="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          TITLE="[deploy-broken] terraform-apply failed on main (${GITHUB_SHA:0:8})"

          BODY_FILE="$(mktemp)"
          {
            echo "**Post-merge terraform-apply failure** on main at \`${TODAY}\` UTC."
            echo ""
            echo "- Failing SHA: \`${GITHUB_SHA}\`"
            echo "- Actions run: https://github.com/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
            echo "- Job: \`terraform-apply\` (deploy-production.yml)"
            echo ""
            echo "## Impact"
            echo ""
            echo "- \`deploy\` job is BLOCKED (Codex F6 strict gate)"
            echo "- Cloud Run 新 revision は deploy されない (build & migrate も skipped)"
            echo "- 以降の PR merge も \`Main Terraform Health\` check により block される"
            echo "  (\`.github/workflows/check-main-terraform-health.yml\`)"
            echo ""
            echo "## Recovery"
            echo ""
            echo "1. Actions run の log で失敗原因を特定 (\`Terraform plan\` step 出力を確認)"
            echo "2. 修正 PR を作成 (commit message に \`[skip-main-health]\` を含めて health gate bypass)"
            echo "3. 修正 PR を merge → 次回 deploy-production run で復旧確認"
            echo "4. 復旧確認できたら本 issue を close"
            echo ""
            echo "## References"
            echo ""
            echo "- Root cause runbook: docs/runbooks/deploy-recovery.md (未作成なら別 PR で作成推奨)"
            echo "- Nightly drift detection: \`.github/workflows/terraform-drift.yml\`"
          } > "${BODY_FILE}"

          gh issue create \
            --repo "${GITHUB_REPOSITORY}" \
            --title "${TITLE}" \
            --label "deploy-broken" \
            --body-file "${BODY_FILE}"

  deploy:
    name: Cloud Build Deploy
    needs: terraform-apply
    if: github.repository == 'y2ikgm89/myrrh-rental-space' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        with:
          workload_identity_provider: ${{ env.WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ env.BUILD_SERVICE_ACCOUNT }}
          project_id: ${{ env.PROJECT_ID }}

      - name: Set up gcloud
        uses: google-github-actions/setup-gcloud@aa5489c8933f4cc7a4f7d45035b3b1440c9c10db # v3.0.1
        with:
          project_id: ${{ env.PROJECT_ID }}
          install_components: beta

      - name: Submit Cloud Build
        shell: bash
        run: |
          set -euo pipefail

          BREAKING_MIGRATION_DEPLOY="false"
          BASE_SHA="${{ github.event.before }}"
          if [ -z "${BASE_SHA}" ] || [[ "${BASE_SHA}" =~ ^0+$ ]] || ! git cat-file -e "${BASE_SHA}^{commit}" 2>/dev/null; then
            BASE_SHA="$(git rev-parse "${GITHUB_SHA}^" 2>/dev/null || git rev-list --max-parents=0 HEAD)"
          fi

          mapfile -t changed_migrations < <(
            git diff --name-only "${BASE_SHA}" "${GITHUB_SHA}" -- 'prisma/migrations/**/migration.sql'
          )

          if ((${#changed_migrations[@]} > 0)); then
            if grep -Eiq 'ALTER[[:space:]]+TABLE[[:space:]]+.*(DROP[[:space:]]+COLUMN|RENAME[[:space:]]+COLUMN|RENAME[[:space:]]+TO)|DROP[[:space:]]+TABLE|DROP[[:space:]]+TYPE' "${changed_migrations[@]}"; then
              BREAKING_MIGRATION_DEPLOY="true"
              printf 'Breaking migration deploy mode enabled for:\n'
              printf '  %s\n' "${changed_migrations[@]}"
            fi
          fi

          SHORT_SHA="${GITHUB_SHA::7}"
          SUBSTITUTIONS="SHORT_SHA=${SHORT_SHA}"
          SUBSTITUTIONS+=",_REGION=${REGION}"
          SUBSTITUTIONS+=",_SERVICE_NAME=${SERVICE_NAME}"
          SUBSTITUTIONS+=",_ADMIN_SERVICE_NAME=${ADMIN_SERVICE_NAME}"
          SUBSTITUTIONS+=",_IAP_JWT_AUDIENCE=${IAP_JWT_AUDIENCE}"
          SUBSTITUTIONS+=",_ADMIN_ROLE_GROUP_SUPER_ADMIN_EMAIL=${ADMIN_ROLE_GROUP_SUPER_ADMIN_EMAIL}"
          SUBSTITUTIONS+=",_ADMIN_ROLE_GROUP_ADMIN_EMAIL=${ADMIN_ROLE_GROUP_ADMIN_EMAIL}"
          SUBSTITUTIONS+=",_ADMIN_ROLE_GROUP_EDITOR_EMAIL=${ADMIN_ROLE_GROUP_EDITOR_EMAIL}"
          SUBSTITUTIONS+=",_ADMIN_ROLE_GROUP_VIEWER_EMAIL=${ADMIN_ROLE_GROUP_VIEWER_EMAIL}"
          SUBSTITUTIONS+=",_REPOSITORY=${AR_REPOSITORY}"
          SUBSTITUTIONS+=",_WORKER_POOL=${WORKER_POOL}"
          SUBSTITUTIONS+=",_SERVICE_ACCOUNT=${RUNTIME_SERVICE_ACCOUNT}"
          SUBSTITUTIONS+=",_BUILD_SERVICE_ACCOUNT=${BUILD_SERVICE_ACCOUNT}"
          SUBSTITUTIONS+=",_NEXT_PUBLIC_BASE_URL=${PUBLIC_DOMAIN}"
          SUBSTITUTIONS+=",_NEXT_PUBLIC_APP_URL=${PUBLIC_DOMAIN}"
          SUBSTITUTIONS+=",_BETTER_AUTH_URL=${PUBLIC_DOMAIN}"
          SUBSTITUTIONS+=",_ADMIN_APP_URL=${ADMIN_DOMAIN}"
          SUBSTITUTIONS+=",_CRON_OIDC_AUDIENCE=${CRON_OIDC_AUDIENCE}"
          SUBSTITUTIONS+=",_CRON_SERVICE_ACCOUNT_EMAIL=${CRON_SERVICE_ACCOUNT_EMAIL}"
          SUBSTITUTIONS+=",_NEXT_PUBLIC_TURNSTILE_SITE_KEY=${NEXT_PUBLIC_TURNSTILE_SITE_KEY}"
          SUBSTITUTIONS+=",_DATABASE_URL_SECRET_VERSION=1"
          SUBSTITUTIONS+=",_BREAKING_MIGRATION_DEPLOY=${BREAKING_MIGRATION_DEPLOY}"
          SUBSTITUTIONS+=",_BETTER_AUTH_SECRET_VERSION=1"
          SUBSTITUTIONS+=",_ENCRYPTION_KEY_SECRET_VERSION=1"
          SUBSTITUTIONS+=",_AUDIT_LOG_HMAC_KEY_SECRET_VERSION=1"
          SUBSTITUTIONS+=",_NEXT_SERVER_ACTIONS_ENCRYPTION_KEY_SECRET_VERSION=1"
          SUBSTITUTIONS+=",_R2_ACCOUNT_ID_SECRET_VERSION=1"
          SUBSTITUTIONS+=",_R2_ACCESS_KEY_ID_SECRET_VERSION=1"
          SUBSTITUTIONS+=",_R2_SECRET_ACCESS_KEY_SECRET_VERSION=1"
          SUBSTITUTIONS+=",_R2_BUCKET_NAME_SECRET_VERSION=1"
          SUBSTITUTIONS+=",_R2_PUBLIC_URL_SECRET_VERSION=1"
          SUBSTITUTIONS+=",_CLOUDFLARE_ZONE_ID_SECRET_VERSION=1"
          SUBSTITUTIONS+=",_CLOUDFLARE_API_TOKEN_SECRET_VERSION=1"
          SUBSTITUTIONS+=",_CLOUDFLARE_ORIGIN_HEADER_SECRET_VERSION=1"
          SUBSTITUTIONS+=",_GOOGLE_CLIENT_ID_SECRET_VERSION=1"
          SUBSTITUTIONS+=",_GOOGLE_CLIENT_SECRET_SECRET_VERSION=1"

          gcloud beta builds submit \
            --project="${PROJECT_ID}" \
            --region="${REGION}" \
            --config=cloudbuild.yaml \
            --substitutions="${SUBSTITUTIONS}"
